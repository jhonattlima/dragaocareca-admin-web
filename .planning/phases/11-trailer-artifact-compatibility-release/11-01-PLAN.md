---
phase: 11-trailer-artifact-compatibility-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/core/api.service.ts
  - src/app/pages/manage/manage.component.ts
autonomous: true
requirements: [ARTIFACT-01, ARTIFACT-02]
must_haves:
  truths:
    - "The Angular artifact selector type accepts the API's exact trailer-video selector without changing existing selector values or endpoints."
    - "The Episodes download modal renders a Trailer video .mp4 option from the generic artifact catalog and enables it only when the episode DTO exposes a finalized trailerVideoFileName."
    - "Staged upload state, selected browser Files, YouTube state, and client filenames never make a trailer-video option available or alter the backend-owned artifact flow."
    - "The existing generic start, poll, authenticated Blob delivery, retry, reset, and canonical-header behavior remain the only artifact flow."
  artifacts:
    - path: "src/app/core/api.service.ts"
      provides: "EpisodeArtifactSelector union including the canonical trailer-video request value and unchanged job DTO/wrappers."
    - path: "src/app/pages/manage/manage.component.ts"
      provides: "Shared artifact definition and option generation mapped to Episode.trailerVideoFileName."
  key_links:
    - from: "EpisodeArtifactSelector"
      to: "ApiService.startEpisodeArtifactJob"
      via: "typed artifacts array posts the exact selector trailer-video"
    - from: "Episode.trailerVideoFileName"
      to: "ManageComponent.buildArtifactOptions"
      via: "artifact definition fileField availability hint"
    - from: "ManageComponent artifact options"
      to: "existing artifact job/poll/delivery methods"
      via: "generic checked selector mapping and existing lifecycle"
---

<objective>
Add the minimal Angular contract/catalog integration for finalized local trailer-video artifacts. This exposes the API-supported selector in the existing Episodes modal and lets the existing artifact job and ZIP flow carry it without a second download path or browser-side media logic.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/README.md
@docs/ARCHITECTURE.md
@docs/CONFIGURATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.planning/phases/11-trailer-artifact-compatibility-release/11-RESEARCH.md
@.planning/phases/10-operator-workflow-integration/10-01-SUMMARY.md
@.planning/phases/10-operator-workflow-integration/10-02-SUMMARY.md
@.planning/phases/10-operator-workflow-integration/10-03-SUMMARY.md
@src/app/core/api.service.ts
@src/app/pages/manage/manage.component.ts
@src/app/pages/manage/manage.component.html
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extend the typed artifact selector contract</name>
  <files>src/app/core/api.service.ts</files>
  <action>Read the current dirty diff before editing and preserve every unrelated Phase 9/10 hunk. Extend EpisodeArtifactSelector with exactly the API selector literal `trailer-video`; retain all existing selectors, EpisodeArtifactJobSnapshot fields, POST route, status route, authenticated Blob download method, URL normalization, and interceptor behavior. Do not add a new endpoint, DTO field, client ZIP implementation, filename/path input, or API business rule. The backend remains authoritative for finalized-file preflight, missing/staged/stale handling, canonical ZIP entries, and authenticated delivery.</action>
  <verify><automated>./node_modules/.bin/tsc -p tsconfig.app.json --noEmit && ./node_modules/.bin/tsc -p tsconfig.spec.json --noEmit</automated></verify>
  <done>TypeScript accepts `trailer-video` anywhere the existing artifact selector union is used, while the API wrapper and all other selectors remain behaviorally unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add finalized trailer-video to the shared modal catalog</name>
  <files>src/app/pages/manage/manage.component.ts</files>
  <action>Preserve unrelated dirty ManageComponent changes and edit only the artifact definition/file-field typing and option-generation seam. Extend the ArtifactDefinition fileField union to include `trailerVideoFileName`, then add one catalog entry in the established API/catalog order after the trailer-audio row with selector `trailer-video`, label `Trailer video`, and format hint `.mp4`. Keep buildArtifactOptions generic: availability is based only on a trimmed nonempty Episode.trailerVideoFileName display hint, checked defaults follow the existing active-job selection behavior, and unavailable options stay disabled with the existing message. Do not inspect selected File objects, trailer upload state, YouTube state, staged paths, or infer finalization in Angular; Save/create/API finalization remains the lifecycle boundary. Do not add selector-specific HTML or alter polling, stale job-ID guards, partial/missing messaging, native download, retry, or reset logic.</action>
  <verify><automated>./node_modules/.bin/tsc -p tsconfig.app.json --noEmit && rg -n "trailer-video|trailerVideoFileName|Trailer video" src/app/pages/manage/manage.component.ts</automated></verify>
  <done>The generic Episodes modal can produce a visible Trailer video .mp4 option, enables/checks it only for a finalized filename hint, and leaves staged/absent trailer videos unavailable without changing the generic artifact lifecycle.</done>
</task>
</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Episodes downloads include finalized local trailer video through the existing artifact job and ZIP flow without staged/stale media exposure | 11-01, 11-02 | COVERED | Contract/catalog integration plus API-preflight and regression gates cover the goal.
REQ | ARTIFACT-01 | Modal offers finalized trailer-video when available | 11-01, 11-02 | COVERED | Typed selector, catalog row, DTO availability hint, and focused UI assertions.
REQ | ARTIFACT-02 | ZIP contains canonical finalized trailer.mp4 and excludes unavailable/staged files | 11-01, 11-02 | COVERED | Client sends exact selector; sibling API preflight/verifier remains authoritative and is rerun in 11-02.
RESEARCH | R-01 | Exact selector is trailer-video; no new endpoint or DTO expansion | 11-01 | COVERED | Task 1 preserves the existing ApiService job contract.
RESEARCH | R-02 | Availability uses finalized DTO filename only as a display hint; API preflight is authoritative | 11-01, 11-02 | COVERED | Task 2 avoids staged/client-file inference; tests cover unavailable and backend rejection.
RESEARCH | R-03 | Generic polling/native delivery/retry/reset must remain intact | 11-02 | COVERED | Regression suite and build gates exercise existing flow without new delivery code.
RESEARCH | R-04 | Stale replacement safety is backend evidence/hash validation, not client path logic | 11-01, 11-02 | COVERED | Plan preserves backend boundary and tests recoverable unavailable behavior.
CONTEXT | user constraints | Minimal Angular/API integration; no backend redesign, client ZIP, or unrelated dirty-source changes | 11-01, 11-02 | COVERED | Exact file scope and preservation instructions are repeated in both plans.
</source_coverage_audit>

<verification>
Wave 1 must compile before Wave 2. Existing known Angular selector-parser and bundle/style budget warnings are non-blocking only if the commands exit successfully; any new TypeScript/template error is a failure.
</verification>

<success_criteria>
The frontend can represent and display the API-supported finalized trailer-video artifact while retaining backend ownership of availability, stale safety, archive naming, and delivery.
</success_criteria>

<output>Create `.planning/phases/11-trailer-artifact-compatibility-release/11-01-SUMMARY.md` when executed.</output>
