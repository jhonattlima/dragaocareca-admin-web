---
phase: 06-browser-download-release-validation
plan: 04
type: execute
wave: 1
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - src/app/pages/manage/manage.component.ts
  - src/app/pages/manage/manage.component.html
  - src/app/pages/manage/manage.component.spec.ts
  - src/app/pages/manage/phase6-validation.spec.ts
  - scripts/phase6-validation.js
  - .planning/phases/06-browser-download-release-validation/06-VALIDATION.md
  - .planning/phases/06-browser-download-release-validation/06-VERIFICATION.md
  - .planning/phases/06-browser-download-release-validation/06-UAT.md
  - .planning/v1.1-MILESTONE-AUDIT.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [UI-01, UI-06, UI-07, UI-08, VAL-01, VAL-02, VAL-03]
must_haves:
  truths:
    - "The existing ManageComponent suite passes focused UI-01, UI-06, polling-cleanup, download/retry/reset, and Add episode isolation assertions in a real ChromeHeadless Karma run."
    - "The complete browser validation matrix either produces bounded, reproducible evidence against the real DC334 fixture/API or records each unsupported scenario with its exact reason; no scenario is represented as passed without browser evidence."
    - "Successful native delivery, partial warning, failed preparation, network/auth/expiry recovery, completed reopen, same-job delivery retry, reset/new selection, repeated-completion idempotence, focus restoration, and cleanup remain observable without duplicate preparation jobs or downloads."
    - "Phase 6 verification, validation, UAT, requirements, and the v1.1 milestone audit agree on the actual Karma, browser, build, dependency, fixture, and cleanup results."
  artifacts:
    - path: "src/app/pages/manage/manage.component.spec.ts"
      provides: "Corrected focused ManageComponent DOM, focus, polling-teardown, delivery, and editor-isolation coverage"
    - path: "src/app/pages/manage/manage.component.ts"
      provides: "Minimal production polling/focus behavior correction only where failing Karma evidence proves it is required"
    - path: "src/app/pages/manage/phase6-validation.spec.ts"
      provides: "Focused recovery contract coverage retained and runnable with the full ManageComponent suite"
    - path: "scripts/phase6-validation.js"
      provides: "No-new-dependency bounded Playwright harness for real DC334 browser recovery validation and evidence output"
    - path: ".planning/phases/06-browser-download-release-validation/06-VALIDATION.md"
      provides: "Exact commands, browser scenario matrix, unsupported-scenario record, and cleanup evidence"
    - path: ".planning/phases/06-browser-download-release-validation/06-VERIFICATION.md"
      provides: "Reverified Phase 6 must-haves and blocker dispositions"
    - path: ".planning/phases/06-browser-download-release-validation/06-UAT.md"
      provides: "UAT status reconciled with browser evidence"
    - path: ".planning/v1.1-MILESTONE-AUDIT.md"
      provides: "Milestone requirement and release-gap reconciliation"
  key_links:
    - from: "src/app/pages/manage/manage.component.spec.ts"
      to: "src/app/pages/manage/manage.component.ts and manage.component.html"
      via: "DOM assertions exercise the actual Downloads row, invoker focus, polling cleanup, and editor state boundaries"
    - from: "scripts/phase6-validation.js"
      to: "real DC334 fixture and existing sibling API"
      via: "preflight, controlled fixture/API lifecycle, Playwright request observation, and finally-based restoration"
    - from: "scripts/phase6-validation.js"
      to: ".planning/phases/06-browser-download-release-validation/06-VALIDATION.md"
      via: "machine-readable bounded results are transcribed without upgrading unsupported cases"
    - from: "CHROME_BIN-qualified Karma and npm run build"
      to: "06-VERIFICATION.md and v1.1-MILESTONE-AUDIT.md"
      via: "exact exit codes and assertion outcomes drive release-gap status"
---

<objective>
Close the remaining Phase 6 release blockers: make the full existing ManageComponent Karma suite green, replace the timing-sensitive recovery attempt with a deterministic bounded real-browser harness, and reconcile every Phase 6 evidence artifact from the resulting facts.

Purpose: The current focused recovery spec is green, but the full suite still fails UI-01 Downloads markup, UI-06 invoker-focus restoration, and teardown polling behavior that ends in undefined.subscribe and a Chrome disconnect. The prior browser harness stopped at the correctly disabled empty-selection control, so partial availability, failed preparation, delivery failures, reopen/retry/reset, repeated completion, and cleanup have not been evidenced in the real browser. Preserve the already successful native download, retry, reset, progress, fixture, CORS, and Add episode behavior.

Output: Focused-first Karma repairs, a tracked no-new-dependency Playwright harness and result artifact, exact full-suite/build/dependency evidence, and reconciled Phase 6/milestone records. No application source is changed during planning; implementation changes are limited to the files listed in the tasks.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-browser-download-release-validation/06-CONTEXT.md
@.planning/phases/06-browser-download-release-validation/06-RESEARCH.md
@.planning/phases/06-browser-download-release-validation/06-VERIFICATION.md
@.planning/phases/06-browser-download-release-validation/06-VALIDATION.md
@.planning/phases/06-browser-download-release-validation/06-UAT.md
@.planning/phases/06-browser-download-release-validation/06-01-SUMMARY.md
@.planning/phases/06-browser-download-release-validation/06-02-SUMMARY.md
@.planning/phases/06-browser-download-release-validation/06-03-SUMMARY.md
@.planning/v1.1-MILESTONE-AUDIT.md
@docs/README.md
@docs/ARCHITECTURE.md
@docs/CONFIGURATION.md
@docs/TESTING.md
@src/app/pages/manage/manage.component.ts
@src/app/pages/manage/manage.component.html
@src/app/pages/manage/manage.component.spec.ts
@src/app/pages/manage/phase6-validation.spec.ts
@../dragaocareca-admin-api/src/services/episode-artifact-preparation.service.ts
@../dragaocareca-admin-api/src/services/episode-artifact-download.service.ts
@../dragaocareca-admin-api/src/services/episode-media-layout.service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair the full ManageComponent Karma contract and lifecycle cleanup</name>
  <files>src/app/pages/manage/manage.component.ts, src/app/pages/manage/manage.component.html, src/app/pages/manage/manage.component.spec.ts, src/app/pages/manage/phase6-validation.spec.ts</files>
  <behavior>
    - Test 1: UI-01 locates the Downloads header and row action semantically, confirms the icon-only labeled control, and opens the artifact dialog.
    - Test 2: UI-06 confirms Tab/Shift+Tab trapping, Escape close, and restoration of focus to the exact invoker after asynchronous close.
    - Test 3: artifact polling teardown never invokes subscribe on an undefined spy result and leaves no interval or callback that disconnects Chrome after the suite.
    - Test 4: delivery/retry/reset/reopen/repeated-completion behavior and Add episode transcription/summary isolation remain passing.
  </behavior>
  <action>Run the three focused suites first with the existing Playwright Chromium path in CHROME_BIN: manage.component.spec.ts, phase6-validation.spec.ts, and phase6-progress-threshold.spec.ts. Capture the exact failing expectation and stack before editing. Correct the UI-01 assertion or template only according to the observed DOM contract; do not weaken the requirement or hide a missing Downloads row. Correct the UI-06 focus test/component timing seam so it waits for Angular's existing deferred restoration and asserts the original button, while preserving native dialog trapping and Escape behavior. Trace undefined.subscribe and Chrome disconnect to the responsible polling/start/status spy or production teardown path, then make the smallest evidence-backed fix so every HTTP Observable is defined, terminal polling is cleared, destruction is safe, and Add episode transcription/summary polling remains isolated from artifact polling. Preserve D-01 authenticated Blob delivery, D-02 exactly-once automatic native download, D-03 server-authoritative filename, D-04 object-URL revocation, and D-05 same-job manual delivery retry. Do not change API contracts, artifact selectors, progress boundaries, or add dependencies.</action>
  <verify>
    <automated>CHROME_BIN=/home/jhonatt/.cache/ms-playwright/chromium-1228/chrome-linux64/chrome npm test -- --watch=false --browsers=ChromeHeadless --include='src/app/pages/manage/manage.component.spec.ts' --include='src/app/pages/manage/phase6-validation.spec.ts' --include='src/app/pages/manage/phase6-progress-threshold.spec.ts'</automated>
  </verify>
  <done>The combined focused ChromeHeadless run exits 0 with UI-01, UI-06, polling cleanup, delivery/recovery, repeated-completion, and Add episode isolation assertions passing; no unrelated production behavior or dependency is introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Build and execute the bounded real-DC334 browser recovery harness</name>
  <files>scripts/phase6-validation.js</files>
  <action>Create a tracked Node/Playwright harness using only the existing playwright package and Node standard-library modules. Accept explicit frontend/API/fixture/report paths, preflight the existing Chromium binary, frontend, sibling API, and real DC334 source/destination, and fail closed with an unsupported result rather than fabricate data or silently substitute a synthetic fixture. Use bounded per-scenario and overall timeouts, deterministic selectors, request/response counters, browser console/page-error capture, and a try/finally cleanup path. Reuse the existing authBypass pairing and sibling API worker/runbook; snapshot the episode-334 row, media destination, API artifact/job state, and source metadata before staging, never mutate the immutable source, restore row/configuration/media destination, remove temporary validation state, and record cleanup verification.

Exercise and record independent scenario results for partial availability warning plus ZIP; failed preparation; network failure; 401, 403, 404, and 409 delivery; completed modal reopen; same-job delivery retry with no second preparation POST; reset/start-new-selection; repeated completed status exactly once; and modal cleanup/focus restoration. Handle empty selection by asserting Prepare is disabled without clicking it, then continue or record the next scenario unsupported with the exact blocker. For each supported case record visible state, preparation/download counts, HTTP status, native filename, object URL create/activate/revoke counts, and cleanup. The harness may use existing API verifier controls or reversible runtime fixture mutations for partial/failure cases, but must not edit sibling API source, install a package, bypass the real UI, or claim Creating ZIP/Finalizing ZIP unless observed. Emit a deterministic JSON/Markdown report under an ignored temporary directory for Task 3 to transcribe.</action>
  <verify>
    <automated>node scripts/phase6-validation.js --help && node scripts/phase6-validation.js --report-dir /tmp/phase6-validation-report</automated>
  </verify>
  <done>The harness completes within its bound, produces evidence for every safely executable real-DC334 scenario, marks unavailable prerequisites or unsupported scenarios explicitly, proves duplicate-preparation/download behavior where applicable, and leaves fixture/API state restored.</done>
</task>

<task type="auto">
  <name>Task 3: Run release gates and reconcile all Phase 6 evidence</name>
  <files>.planning/phases/06-browser-download-release-validation/06-VALIDATION.md, .planning/phases/06-browser-download-release-validation/06-VERIFICATION.md, .planning/phases/06-browser-download-release-validation/06-UAT.md, .planning/v1.1-MILESTONE-AUDIT.md, .planning/REQUIREMENTS.md</files>
  <action>Run the focused Karma command from Task 1, then the CHROME_BIN-qualified full frontend Karma suite, npm run build, and git diff --exit-code -- package.json package-lock.json. Run the harness with the real fixture/API and preserve its raw report. Update 06-VALIDATION.md with exact commands, dates, binary/API/frontend configuration, assertion counts, browser outcomes, request counts, ZIP/header evidence, cleanup checks, warnings, and every unsupported scenario. Update 06-VERIFICATION.md by closing only gaps directly supported by evidence; update 06-UAT.md only where browser evidence changes its status, retaining explicit human-needed or unsupported labels; reconcile .planning/REQUIREMENTS.md VAL-02/VAL-03 and .planning/v1.1-MILESTONE-AUDIT.md with the actual acceptance state. Preserve prior full-selection DC334, CORS, filename, progress-threshold, native delivery, retry/reset, fixture restoration, and Add episode evidence. Do not convert a focused/mock pass into real-browser proof, claim unsupported scenarios, edit sibling API source, alter manifests, or alter the immutable fixture.</action>
  <verify>
    <automated>CHROME_BIN=/home/jhonatt/.cache/ms-playwright/chromium-1228/chrome-linux64/chrome npm test -- --watch=false --browsers=ChromeHeadless && npm run build && git diff --exit-code -- package.json package-lock.json</automated>
  </verify>
  <done>All release records agree on full Karma, build/dependency checks, real-browser recovery, unsupported limitations, and cleanup. Requirements are marked complete only when acceptance evidence is genuinely satisfied; otherwise the remaining blocker is explicit and traceable to raw output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Angular DOM/test harness → ManageComponent state | Browser events and test-controlled Observables drive UI transitions and polling cleanup. |
| Browser → sibling API | Authenticated job, status, and Blob requests cross the browser/API boundary and may return network/auth/expiry failures. |
| Fixture/API runtime → validation evidence | Temporary media and database state must be isolated, restored, and reported without exposing tokens. |
| Repository dependency manifests → validation runtime | Tests use the existing package set and Chromium binary only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-06-12 | Tampering | ManageComponent polling and retry state | high | mitigate | Define every test Observable, clear all polling on terminal state/destroy, preserve job identity guards, and require focused/full ChromeHeadless success. |
| T-06-13 | Spoofing | Browser/API auth and delivery failure matrix | high | mitigate | Use the existing authBypass pairing or real bearer path, observe actual statuses, and record 401/403/404/409/network outcomes without replacing them with success evidence. |
| T-06-14 | Denial of Service | Playwright harness and fixture/API runtime | medium | mitigate | Apply per-scenario/overall deadlines, serialize mutations, stop child processes in finally, restore snapshots, and fail closed on stale pre-state. |
| T-06-15 | Information Disclosure | Validation reports and browser traces | medium | mitigate | Redact tokens/cookies and report only required metadata, statuses, filenames, counts, and cleanup outcomes. |
| T-06-16 | Tampering | Real DC334 source and API media/database state | high | mitigate | Treat source as immutable, snapshot row/media state, use dedicated temporary state, verify restoration, and remove temporary artifacts. |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | Do not install dependencies; run the package/lockfile diff gate and use the declared Playwright package and existing binary. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Browser-native ZIP delivery with understandable recovery and DC334 validation | 06-04 | COVERED | Tasks 1–3 close runtime debt and browser evidence gaps while retaining prior happy-path evidence. |
| REQ | UI-01 | Downloads action on every episode row | 06-04 | COVERED | Task 1 diagnoses the failing DOM assertion against actual Downloads markup. |
| REQ | UI-06 | Progress and duplicate-submission protection | 06-04 | COVERED | Task 1 reruns polling/start lifecycle and Add episode isolation. |
| REQ | UI-07 | Automatic native ZIP download | 06-04 | COVERED | Task 1 preserves D-01–D-05; Task 2 observes real delivery and cleanup. |
| REQ | UI-08 | Empty, partial, failure, reset, retry, reopen, repeated-completion recovery | 06-04 | COVERED | Task 2 executes each scenario or records it unsupported with evidence. |
| REQ | VAL-01 | Real Season 3 DC334 fixture | 06-04 | COVERED | Task 2 preflights, uses, snapshots, and restores the existing real fixture/API state. |
| REQ | VAL-02 | Complete UI/browser validation and ZIP contents | 06-04 | COVERED | Task 2 provides deterministic browser evidence; unsupported cases remain explicit. |
| REQ | VAL-03 | Frontend tests and build | 06-04 | COVERED | Tasks 1 and 3 run focused/full Chrome Karma, build, and dependency checks. |
| RESEARCH | R-06-01 | Backend-owned selectors, authenticated Blob, safe filename/header handling | 06-04 | COVERED | Existing API boundary is preserved; no client ZIP or filename fabrication. |
| RESEARCH | R-06-02 | Real fixture restoration and timing-safe browser validation | 06-04 | COVERED | Pre-state snapshots, bounded waits, deterministic selectors, and finally cleanup. |
| RESEARCH | R-06-03 | No new E2E dependency; existing Playwright/Karma path | 06-04 | COVERED | Existing package is used and manifests are checked unchanged. |
| CONTEXT | D-01 | Authenticated Angular HttpClient Blob delivery | 06-04 | COVERED | Task 1 preserves interceptor-backed requests. |
| CONTEXT | D-02 | One automatic native browser download at completion | 06-04 | COVERED | Tasks 1 and 2 assert exactly-once delivery. |
| CONTEXT | D-03 | Content-Disposition filename authority | 06-04 | COVERED | Task 2 records actual header/native filename. |
| CONTEXT | D-04 | Temporary object URL revocation | 06-04 | COVERED | Existing cleanup is preserved and observed. |
| CONTEXT | D-05 | Same-job delivery retry without another job | 06-04 | COVERED | Tasks 1 and 2 count preparation POSTs and delivery retries. |
</source_audit>

<verification>
<automated>CHROME_BIN=/home/jhonatt/.cache/ms-playwright/chromium-1228/chrome-linux64/chrome npm test -- --watch=false --browsers=ChromeHeadless --include='src/app/pages/manage/manage.component.spec.ts' --include='src/app/pages/manage/phase6-validation.spec.ts' --include='src/app/pages/manage/phase6-progress-threshold.spec.ts'</automated>
<automated>node scripts/phase6-validation.js --report-dir /tmp/phase6-validation-report</automated>
<automated>CHROME_BIN=/home/jhonatt/.cache/ms-playwright/chromium-1228/chrome-linux64/chrome npm test -- --watch=false --browsers=ChromeHeadless</automated>
<automated>npm run build</automated>
<automated>git diff --exit-code -- package.json package-lock.json</automated>
<human-check>Review the raw harness report and confirm every pass has browser evidence, every unsupported scenario has a concrete reason, ZIP entries match selected real DC334 files, and fixture/API cleanup restored pre-state.</human-check>
</verification>

<success_criteria>
- Focused and full CHROME_BIN-qualified Karma runs exit 0, including UI-01, UI-06, polling teardown/Chrome disconnect, delivery/retry/reset, and Add episode isolation.
- The bounded Playwright harness uses real DC334 state without synthetic evidence, captures each requested recovery scenario or records it unsupported, and proves request counts and cleanup where applicable.
- npm run build passes and the package/lockfile diff is clean with no dependency additions.
- 06-VALIDATION.md, 06-VERIFICATION.md, 06-UAT.md, REQUIREMENTS.md, and the milestone audit contain mutually consistent evidence-backed statuses and limitations.
- Existing native download, server filename, CORS, progress-threshold, partial-result, retry, reset, repeated-completion, object-URL cleanup, fixture restoration, and Add episode isolation behavior is preserved.
</success_criteria>

<output>
Create .planning/phases/06-browser-download-release-validation/06-04-SUMMARY.md when done, including focused/full Karma results, harness report path, per-scenario status, cleanup proof, build/dependency results, and remaining unsupported blockers.
</output>
