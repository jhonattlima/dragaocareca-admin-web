---
phase: 09-title-hashtag-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../dragaocareca-admin-api/src/routes/episodes.routes.ts
  - ../dragaocareca-admin-api/src/services/youtube-trailer-job.service.ts
  - ../dragaocareca-admin-api/src/services/youtube-trailer-publication.service.ts
  - ../dragaocareca-admin-api/src/config/env.ts
  - ../dragaocareca-admin-api/.env.example
  - ../dragaocareca-admin-api/src/docs/openapi.ts
  - ../dragaocareca-admin-api/src/scripts/verify-episode-hashtag-authoring.ts
autonomous: true
requirements: [TITLE-01, TITLE-02, TITLE-03, TITLE-04]
must_haves:
  truths:
    - "The API accepts the authored hashtag values and the computed Trailer title at the existing start and Save/commit publication boundary, rather than rebuilding publication metadata from unrelated legacy tags."
    - "The API rejects an assembled trailer title over 100 Unicode characters and invalid hashtag tokens without mutating the authored episode name or hashtags."
    - "Normalized hashtag lookup returns approximate count, retrieval time, cache state, and safe recoverable unavailable/quota responses."
    - "Successful and zero-result normalized hashtag cache entries use an explicit approximately one-hour freshness policy."
  artifacts:
    - "Sibling API route schemas and commit handler define one canonical title/hashtag metadata contract."
    - "Sibling API environment defaults and verification script cover one-hour cache expiry and title/hashtag lifecycle behavior."
  key_links:
    - "Save/update persistence or the authenticated commit request must carry the exact authored hashtag values into requestPublication and provider publication."
    - "The same assembled-title validator is applied at start and commit/publication boundaries."
    - "The lookup route exposes only the existing safe DTO while cache/quota/provider details remain API-owned."
---

<objective>
Correct the sibling API contract that Phase 9 consumes: make the authored hashtag field and read-only assembled title durable across Save/start/commit, enforce the shared 100-Unicode-character rule, and set/test the one-hour normalized lookup cache policy.

Purpose: D-01/D-02 make the preview read-only and D-07/D-09 require authored hashtag text to survive automatic suggestions and Save. The API is the durable authority, so Angular must not be the only place where these values exist.
Output: API route/schema/config/OpenAPI changes plus an executable hashtag-authoring verifier covering lifecycle, validation, lookup, cache, quota, and safe error behavior.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-title-hashtag-authoring/09-CONTEXT.md
@.planning/phases/09-title-hashtag-authoring/09-RESEARCH.md
@docs/ARCHITECTURE.md
@docs/CONFIGURATION.md
@../dragaocareca-admin-api/src/routes/episodes.routes.ts
@../dragaocareca-admin-api/src/services/episode-summary.service.ts
@../dragaocareca-admin-api/src/database/repositories/youtube-hashtag-cache.repository.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Make Save/start/commit metadata canonical and validate assembled title</name>
  <files>../dragaocareca-admin-api/src/routes/episodes.routes.ts, ../dragaocareca-admin-api/src/services/youtube-trailer-job.service.ts, ../dragaocareca-admin-api/src/services/youtube-trailer-publication.service.ts, ../dragaocareca-admin-api/src/docs/openapi.ts, ../dragaocareca-admin-api/src/scripts/verify-episode-hashtag-authoring.ts</files>
  <behavior>
    - Test 1: a saved episode's authored hashtags and computed read-only Trailer title are the metadata used by the existing start and commit/publication flow.
    - Test 2: a title at the 100-Unicode-character boundary is accepted and a title above it is rejected at both relevant API boundaries.
    - Test 3: malformed hashtag tokens, duplicate/over-limit values, and invalid title inputs return safe validation errors without replacing authored values.
    - Test 4: a commit remains idempotent and does not introduce a second publication route or browser/provider contract.
  </behavior>
  <action>Use the smallest durable contract: extend the authenticated commit body to accept `{ title, hashtags }` alongside the existing optional `jobId`; validate and persist those values through the existing YouTube job `metadata_snapshot_json` via `requestPublication`, then publish from that snapshot. Keep the pre-Save start body compatible for the upload-time snapshot, but make Save-time commit metadata authoritative and retry/reload-safe. Implement the exact read-only preview format from D-01/D-02 (`Trailer - {episode name}` followed by up to three authored hashtag tokens), and place the shared assembled-title validator in `youtube-trailer-publication.service.ts`, reusing it from start/commit/publication boundaries. The validator counts Unicode code points (`[...value].length`) and checks the complete assembled string at 100/101 boundaries plus forbidden characters. Ensure the legacy generic episode tags are not silently substituted for the separate authored hashtag field. Update routes, job/publication services, OpenAPI, and verifier fixtures/assertions to prove save payload → requestPublication metadata snapshot → reload/retry/publication metadata. Do not make the title editable, add browser-side provider calls, or create a second publication flow. This plan owns API changes in the sibling repository; execute them from `../dragaocareca-admin-api`, run its build/verifiers there, and preserve unrelated sibling work.</action>
  <verify><automated>cd ../dragaocareca-admin-api && npm run build && NODE_ENV=development DISABLE_BACKGROUND_WORKERS=true npm run verify:episode-hashtag-authoring -- --focus=lifecycle</automated></verify>
  <done>API lifecycle tests prove D-01/D-02/D-03/D-04 and D-06/D-09: authored hashtags survive Save/reload, the assembled title is authoritative and bounded at 100 Unicode characters, invalid data is rejected without mutation, and existing start/commit publication remains the only flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Set one-hour hashtag cache freshness and lookup recovery coverage</name>
  <files>../dragaocareca-admin-api/src/config/env.ts, ../dragaocareca-admin-api/.env.example, ../dragaocareca-admin-api/src/docs/openapi.ts, ../dragaocareca-admin-api/src/scripts/verify-episode-hashtag-authoring.ts</files>
  <behavior>
    - Test 1: equivalent NFKC/case/leading-hash inputs share one normalized cache key and a fresh hit avoids a provider call.
    - Test 2: successful and zero-result entries remain hits within the configured one-hour window and become misses immediately after expiry.
    - Test 3: unavailable, quota, invalid-input, and provider responses remain safe, recoverable DTOs with retry metadata and no sensitive fields.
    - Test 4: automatic and manual lookup lanes retain their existing bounded quota and serialization behavior.
  </behavior>
  <action>Change the checked-in API cache defaults and documented configuration to an explicit approximately one-hour freshness policy for successful and zero-result normalized lookups, preserving the API's configurable bounds and deciding/documenting the error-result TTL consistently. Extend the existing disposable-fixture verifier with an injected clock boundary at one hour, normalized repeated lookup assertions, safe unavailable/quota assertions, and the existing one-lane/quota checks. Keep cache, quota, provider credentials, and raw provider responses server-side per D-10 through D-13 and the architectural responsibility map.</action>
  <verify><automated>cd ../dragaocareca-admin-api && npm run build && NODE_ENV=development DISABLE_BACKGROUND_WORKERS=true npm run verify:episode-hashtag-authoring -- --focus=lookup</automated></verify>
  <done>One-hour cache behavior is represented in API config/example/docs and passes hit/expiry tests; lookup returns approximate/retrieval/cache metadata and recoverable failure states while preserving server-owned quota/provider boundaries.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated browser → episode API | Untrusted title, hashtag, job, and lookup input crosses into persistence/publication code. |
| API → YouTube/provider and SQLite cache | Server-owned credentials, quota admission, normalized cache keys, and provider responses cross internal boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | start/commit metadata routes | high | mitigate | Validate the assembled title and canonical hashtag tokens at both boundaries and use the durable authored representation for publication. |
| T-09-02 | Information disclosure | lookup DTO/routes | high | mitigate | Retain the allowlisted approximate-count DTO and exclude credentials, prompts, transcripts, raw provider payloads, and cache-ledger details. |
| T-09-03 | Denial of service | hashtag lookup/cache/quota | medium | mitigate | Preserve normalized shared cache, one lookup lane, bounded timeout/retry, and separate manual/automatic admissions; verify cache hits and quota exhaustion. |
| T-09-SC | Tampering | npm installs | high | accept | No package-manager install is planned; existing dependencies are reused and the verifier/build gate detects accidental dependency drift. |
</threat_model>

<verification>
Run the focused API verifier for lifecycle and lookup, then run the full sibling API build and verification script. Confirm the OpenAPI contract describes read-only assembled title semantics, authored hashtags, approximate counts, recoverable errors, and one-hour cache freshness.
</verification>

<success_criteria>
The sibling API is the authoritative, tested contract for authored hashtags, 100-Unicode title validation, approximate lookup/cache behavior, and Save/start/commit publication metadata without any browser-side provider flow.
</success_criteria>

<output>
Create `.planning/phases/09-title-hashtag-authoring/09-01-SUMMARY.md` when done.
</output>
