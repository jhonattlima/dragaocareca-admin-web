---
phase: 07-final-trailer-video-upload
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - docs/README.md
  - docs/ARCHITECTURE.md
  - docs/CONFIGURATION.md
  - .planning/phases/07-final-trailer-video-upload/07-VALIDATION.md
autonomous: true
requirements: [TRAILER-01, TRAILER-02, TRAILER-03, TRAILER-04, TRAILER-05]
must_haves:
  truths:
    - "Project documentation describes the final trailer-video endpoint, immediate draft staging, authenticated ownership, and staged-to-final promotion boundary consistently."
    - "Phase validation records focused backend and Angular automated coverage plus the required full test/build gates and manual recovery check."
    - "The documented Phase 7 boundary explicitly excludes YouTube upload, publishing, hashtags, title generation, and artifact-download integration."
  artifacts:
    - "docs/README.md, docs/ARCHITECTURE.md, and docs/CONFIGURATION.md contain the current local trailer-video contract."
    - "07-VALIDATION.md maps every TRAILER requirement to automated and manual evidence with no watch-mode verification command."
  key_links:
    - "documented POST /episodes/drafts + trailer upload + create lifecycle -> frontend ApiService contract -> sibling API verifier"
    - "validation matrix -> focused tests -> full Karma/build release gate"
---

<objective>
Close the Phase 7 documentation and validation loop after the API and Angular implementation plans execute.

Purpose: Keep the required frontend/backend contract discoverable and ensure the five Phase 7 requirements are proven without accidentally expanding scope into later YouTube phases.
Output: Updated project docs and a signed-off phase validation strategy; no application source changes are made by this plan.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/07-final-trailer-video-upload/07-CONTEXT.md
@.planning/phases/07-final-trailer-video-upload/07-RESEARCH.md
@.planning/phases/07-final-trailer-video-upload/07-VALIDATION.md
@AGENTS.md
@docs/README.md
@docs/ARCHITECTURE.md
@docs/CONFIGURATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the immediate local trailer-video contract and phase boundary</name>
  <files>docs/README.md, docs/ARCHITECTURE.md, docs/CONFIGURATION.md</files>
  <read_first>docs/README.md, docs/ARCHITECTURE.md, docs/CONFIGURATION.md, /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/README.md, .planning/phases/07-final-trailer-video-upload/07-PATTERNS.md</read_first>
  <action>Using the documentation analog paths listed in `.planning/phases/07-final-trailer-video-upload/07-PATTERNS.md`, update only the relevant backend-contract, media-flow, and feature-map sections to document authenticated POST /v1/episodes/drafts with `{episodeId}` -> `{draftId, episodeId, state, expiresAt}`, then POST /v1/episodes/:episodeId/trailer-video with multipart field file plus `X-Episode-Draft-Id`, staged response, and POST /v1/episodes create consuming the same reservation to promote canonical episodes/{id}/trailer.mp4. Document authenticated owner binding, MP4/size authority, rollback/last-known-good semantics, bounded expiration, and authBypass compatibility. State that Angular owns selection/progress/cancel/retry display while the API owns validation, storage, ownership, promotion, cleanup, and persistence. Explicitly mark YouTube transfer, processing, private/public publishing, hashtag lookup, title generation, and trailer artifact download as later phases; do not introduce any of those controls or contracts here.</action>
  <verify><automated>rg -n "trailer-video|draft|staged|episodes/\{?id\}?/trailer.mp4|YouTube|authBypass" docs/README.md docs/ARCHITECTURE.md docs/CONFIGURATION.md</automated></verify>
  <done>All three canonical docs agree with the implemented API/Angular contract and clearly fence Phase 7 from YouTube and artifact features.</done>
</task>

<task type="auto">
  <name>Task 2: Complete Phase 7 validation matrix and final gates</name>
  <files>.planning/phases/07-final-trailer-video-upload/07-VALIDATION.md</files>
  <read_first>.planning/phases/07-final-trailer-video-upload/07-VALIDATION.md, .planning/phases/07-final-trailer-video-upload/07-RESEARCH.md, .planning/phases/07-final-trailer-video-upload/07-CONTEXT.md, .planning/phases/07-final-trailer-video-upload/07-PATTERNS.md</read_first>
  <action>Using the validation/test analog paths in `.planning/phases/07-final-trailer-video-upload/07-PATTERNS.md`, update the validation artifact with actual task/plan IDs, reservation issuance and ownership tests, select-before-Save staged response/create promotion/failure tests, focused sibling API verifier command, focused Angular service/component commands, full Karma command, and npm run build gate. Map TRAILER-01 through TRAILER-05 to automated evidence for route validation/ownership, staging/promotion/cleanup/rollback, reservation/save wiring, byte progress, card states, cancel/retry/replacement races, concrete prior-asset DOM retention, and teardown. Retain a manual real-MP4 recovery check for visible progress and browser cancellation. Set compliance/sign-off fields only after commands pass; record any environment-specific ChromeHeadless adjustment and keep all commands non-watch. Include explicit security evidence for unreserved-ID rejection and an assertion that the phase has no YouTube publishing actions.
</action>
  <verify><automated>cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api &amp;&amp; npm run build &amp;&amp; npm run verify:trailer-video-upload-lifecycle &amp;&amp; cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-web &amp;&amp; npm test -- --watch=false --browsers=ChromeHeadless &amp;&amp; npm run build &amp;&amp; rg -n "TRAILER-0[1-5]|episodes/drafts|draftId|verify:trailer-video-upload-lifecycle|npm run build|YouTube|nyquist_compliant" .planning/phases/07-final-trailer-video-upload/07-VALIDATION.md</automated></verify>
  <done>Validation evidence covers every Phase 7 requirement, full frontend tests/build are green, backend lifecycle verification is named, manual recovery remains explicit, and the validation artifact records the no-YouTube scope boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation -> project documentation | Contract statements can drift from code and mislead future phases. |
| validation commands -> build/test environment | Tooling and browser availability determine whether evidence is reproducible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Tampering | contract documentation | medium | mitigate | Cross-check docs against the API verifier, Angular tests, and exact endpoint/response names before sign-off. |
| T-07-09 | Repudiation | validation evidence | low | mitigate | Record deterministic non-watch commands and requirement-to-test mapping in 07-VALIDATION.md. |
| T-07-11 | Elevation of privilege | reservation/save validation evidence | high | mitigate | Require verifier evidence that unreserved, expired, reused, mismatched, and other-owner draft IDs cannot stage or create media. |
| T-07-SC | Tampering | npm install/build verification | high | mitigate | Do not add dependencies; run existing full Angular test/build and sibling API verifier gates. |
</threat_model>

<verification>
Run the sibling API lifecycle verifier, focused and full Angular Karma suite, and `npm run build`. Confirm docs and validation use the same endpoint, response vocabulary, ownership rule, and final path as the implementation.
</verification>

<success_criteria>
Phase 7’s local trailer upload contract is documented, all five requirements have automated evidence plus the required manual recovery check, the full frontend test/build gates pass, and no YouTube publishing surface has entered the phase.
</success_criteria>

## Must-Haves

- Canonical docs agree on endpoint, ownership, staging, promotion, rollback, and auth behavior.
- Reservation issuance, upload headers, Save/create consumption, promotion, and create failure behavior are covered in the matrix.
- Validation maps TRAILER-01 through TRAILER-05 to automated and manual evidence.
- Full non-watch Angular test and build gates are recorded.
- Later-phase YouTube and artifact-download scope remains explicitly excluded.

<output>
Create `.planning/phases/07-final-trailer-video-upload/07-03-SUMMARY.md` when done.
</output>
