---
phase: 08-youtube-job-lifecycle
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - ../dragaocareca-admin-api/src/scripts/verify-youtube-trailer-job-lifecycle.ts
  - src/app/core/api.service.spec.ts
  - src/app/pages/manage/manage.component.spec.ts
autonomous: true
requirements: [YOUTUBE-01, YOUTUBE-02, YOUTUBE-03, YOUTUBE-04, YOUTUBE-05, OPS-01, OPS-02, OPS-03, OPS-04]
must_haves:
  truths:
    - "Failing executable checks exist before implementation for accepted title/summary job input and sanitized privateWatchUrl output."
    - "Failing executable checks exist for existing API publish idempotency while preserving the no-Angular-publish-controls Phase 8 boundary."
    - "Failing Angular checks define state mapping, reload recovery, polling teardown, retry/cancel, and stale-source behavior before implementation."
  artifacts:
    - "Backend verifier RED scenarios for metadata, private URL, publish idempotency, retained-private cancellation, and sensitive-field omissions."
    - "Angular RED fixtures/specs for API contract and Manage lifecycle state."
  key_links:
    - "RED verifier assertions -> 08-01 backend implementation -> 08-02 evidence completion"
    - "RED Angular specs -> 08-03 wrappers -> 08-04 Manage implementation"
---

<objective>
Create the executable Wave 0 acceptance scaffold for every Phase 8 contract seam before implementation begins.

Purpose: Prevent the literal YOUTUBE-01/YOUTUBE-02 and OPS-03 requirements from being weakened during implementation and make dependent waves genuinely executable.
Output: Intentionally failing backend and Angular tests that define metadata input, private URL output, publish idempotency, cancellation reconciliation, safe DTOs, and UI lifecycle behavior.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/08-youtube-job-lifecycle/08-CONTEXT.md
@.planning/phases/08-youtube-job-lifecycle/08-RESEARCH.md
@.planning/phases/08-youtube-job-lifecycle/08-PATTERNS.md
@.planning/phases/08-youtube-job-lifecycle/08-VALIDATION.md
@../dragaocareca-admin-api/src/scripts/verify-youtube-trailer-job-lifecycle.ts
@src/app/core/api.service.spec.ts
@src/app/pages/manage/manage.component.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backend RED coverage for literal metadata, private URL, publish idempotency, and safety</name>
  <files>../dragaocareca-admin-api/src/scripts/verify-youtube-trailer-job-lifecycle.ts</files>
  <behavior>
    - Start accepts the selected/current operator title and summary, persists them as job input, and provider invocation receives the accepted values.
    - Ready or retained-private snapshots expose sanitized privateWatchUrl but never raw providerVideoId, session URI, paths, credentials, payloads, or raw failures.
    - The existing API publish route remains present and repeated publish requests are idempotent; Phase 8 has no Angular publish controls.
    - Retained-private cancellation exposes provider-video-retained/reconciliation-required guidance without automatic deletion or publication.
  </behavior>
  <read_first>../dragaocareca-admin-api/src/scripts/verify-youtube-trailer-job-lifecycle.ts, ../dragaocareca-admin-api/src/routes/episodes.routes.ts, ../dragaocareca-admin-api/src/docs/openapi.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Add focused failing verifier assertions and fixtures for the literal YOUTUBE-01 metadata payload, persistence/provider handoff, YOUTUBE-02 sanitized privateWatchUrl, OPS-03 publish idempotency, retained-private cancellation guidance, and OPS-01 sensitive-field/log omissions. Keep the test expectations aligned with the existing API-owned publish route; assert its idempotency rather than asserting that the route is absent. The RED command must fail against the current incomplete contract and become the acceptance evidence completed by Plans 01 and 02.</action>
  <verify><automated>cd ../dragaocareca-admin-api && npm run build && npm run verify:youtube-trailer-job-lifecycle</automated></verify>
  <done>Wave 0 contains executable failing backend assertions for all previously disputed contract seams and does not encode a false no-publish-route assumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Angular RED coverage for wrappers and lifecycle state</name>
  <files>src/app/core/api.service.spec.ts, src/app/pages/manage/manage.component.spec.ts</files>
  <behavior>
    - Start sends selected title and summary; returned privateWatchUrl is retained as a safe DTO field.
    - State mapping, determinate transfer, indeterminate processing, reload/current lookup, retry, cancel boundaries, polling teardown, and stale source guards are specified.
    - No Angular Publish control or browser provider call is expected.
  </behavior>
  <read_first>src/app/core/api.service.spec.ts, src/app/pages/manage/manage.component.spec.ts, src/app/core/api.service.ts, .planning/phases/08-youtube-job-lifecycle/08-PATTERNS.md</read_first>
  <action>Add focused failing Jasmine/HttpTestingController cases for the metadata-bearing start request, privateWatchUrl fixture, current lookup/status/retry/cancel routes, and Manage state machine behavior. Keep tests at the Angular boundary: they must prove the wrapper may type/retain the sanitized URL but must not add publish controls or provider SDK calls. Mark these tests as Wave 0 RED scaffolds; Plans 03 and 04 make them pass.</action>
  <verify><automated>npm test -- --watch=false --browsers=ChromeHeadless --include='src/app/core/api.service.spec.ts' --include='src/app/pages/manage/manage.component.spec.ts' && npm run build</automated></verify>
  <done>Angular RED coverage exists for every required lifecycle state and contract seam before wrapper/UI implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture -> API contract | Fixtures model untrusted metadata and provider outcomes. |
| API snapshot -> Angular | Tests constrain the browser-visible allowlist. |
| existing publish API -> repeated callers | Idempotency tests protect the API-owned public boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-00-01 | Tampering | metadata start input | high | mitigate | RED test requires server-owned validation/storage of accepted title/summary without accepting provider/path/credential fields. |
| T-08-00-02 | Information disclosure | snapshot fixtures | critical | mitigate | RED tests forbid provider IDs, sessions, paths, credentials, raw payloads, and raw failures. |
| T-08-00-03 | Tampering | publish route | high | mitigate | RED test requires existing API publish idempotency while Angular remains control-free in Phase 8. |
| T-08-SC | Tampering | npm installs | high | mitigate | No package installation; use existing locked test toolchains. |
</threat_model>

<verification>
Run both RED commands and record that they fail for the missing contract behavior before Wave 1 implementation. After later waves, rerun them as the acceptance suite.
</verification>

<success_criteria>
Wave 0 is executable, failing for the known gaps, and provides unambiguous acceptance tests for YOUTUBE-01 through YOUTUBE-05 and OPS-01 through OPS-04.
</success_criteria>

<output>
Create `.planning/phases/08-youtube-job-lifecycle/08-00-SUMMARY.md` when done.

## Artifacts this phase produces

- Backend RED verifier scenarios for metadata, private URL, publish idempotency, cancellation, and security.
- Angular RED contract/state tests for wrappers, UI mapping, recovery, and stale protection.
</output>
