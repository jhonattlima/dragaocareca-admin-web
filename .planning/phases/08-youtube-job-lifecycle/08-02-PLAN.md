---
phase: 08-youtube-job-lifecycle
plan: 02
type: execute
wave: 2
depends_on: [08-00, 08-01]
files_modified:
  - ../dragaocareca-admin-api/src/scripts/verify-youtube-trailer-job-lifecycle.ts
  - .planning/phases/08-youtube-job-lifecycle/COVERAGE.md
autonomous: true
requirements: [YOUTUBE-01, YOUTUBE-02, YOUTUBE-03, YOUTUBE-04, YOUTUBE-05, OPS-01, OPS-02, OPS-03, OPS-04]
must_haves:
  truths:
    - "The offline verifier proves accepted title/summary job input, sanitized private-watch-link output, retained-private cancellation, and existing API publish idempotency."
    - "Provider/API capabilities have an explicit coverage decision and every opt-out names its owning later phase or security reason."
  artifacts:
    - "The sibling lifecycle verifier contains executable assertions for persistence/restart, start metadata, publish idempotency, cancellation, errors, logs, DTO/OpenAPI, current lookup, and retry."
    - "COVERAGE.md records all provider/API capabilities and explicit opt-outs."
  key_links:
    - "Verifier scenarios -> route/service/repository/provider behavior"
    - "Coverage matrix -> Phase 8 boundary and Phase 9/10/11 handoffs"
---

<objective>
Close the Phase 8 backend evidence gap and record the provider/API coverage boundary.

Purpose: Make the durable lifecycle and the deliberate contract resolutions executable and reviewable before Angular consumes them.
Output: Expanded offline verifier and `COVERAGE.md` with no silent capability omissions.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-youtube-job-lifecycle/08-CONTEXT.md
@.planning/phases/08-youtube-job-lifecycle/08-RESEARCH.md
@.planning/phases/08-youtube-job-lifecycle/08-VALIDATION.md
@../dragaocareca-admin-api/src/scripts/verify-youtube-trailer-job-lifecycle.ts
@../dragaocareca-admin-api/src/docs/openapi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend lifecycle verifier across the resolved contract</name>
  <files>../dragaocareca-admin-api/src/scripts/verify-youtube-trailer-job-lifecycle.ts</files>
  <read_first>../dragaocareca-admin-api/src/scripts/verify-youtube-trailer-job-lifecycle.ts, ../dragaocareca-admin-api/src/routes/episodes.routes.ts, ../dragaocareca-admin-api/src/services/youtube-trailer-job.service.ts, ../dragaocareca-admin-api/src/database/repositories/youtube-trailer-job.repository.ts, ../dragaocareca-admin-api/src/docs/openapi.ts</read_first>
  <action>Extend the existing fake-provider verifier, not a new test harness, to assert: authenticated metadata-bearing start/status/current/retry/cancel, no-store and body validation; accepted title/summary are persisted as job input and reach the provider, while provider/path/credential fields are rejected; private upload uses privacy private; ready and retained-cancel snapshots expose only a validated privateWatchUrl and never providerVideoId/session/source/path/raw messages/reasons/credentials/leases; duplicate starts, same-job retries, and the existing API publish route are idempotent; range/session/provider-video reconciliation survives restart; cancellation distinguishes local-cancelled from provider-video-retained/reconciliation-required; source replacement obsoletes old jobs and rejects late writes; OAuth, quota, timeout, network/provider, invalid-trailer, and reconciliation categories have bounded retry behavior. Assert that Phase 8 exposes no Angular publish controls, but do not assert that the API publish route is absent. Add log-capture assertions that serialized logs omit secrets, session URLs, raw provider payloads, filesystem paths, and stack traces.</action>
  <verify><automated>cd ../dragaocareca-admin-api && npm run build && npm run verify:youtube-trailer-job-lifecycle</automated></verify>
  <done>The verifier fails if any locked D-01 through D-12 contract, YOUTUBE-01/02 resolution, security omission, recovery, idempotency, cancellation, or failure-category behavior regresses.</done>
</task>

<task type="auto">
  <name>Task 2: Produce provider capability coverage and handoff evidence</name>
  <files>.planning/phases/08-youtube-job-lifecycle/COVERAGE.md</files>
  <read_first>.planning/phases/08-youtube-job-lifecycle/08-RESEARCH.md, .planning/phases/08-youtube-job-lifecycle/08-CONTEXT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>Write COVERAGE.md as the API coverage-gate artifact. Inventory each capability decision: authenticated metadata-bearing start, accepted title/summary job input, current lookup/reload recovery, resumable private upload, processing poll, privateWatchUrl, same-job retry, cancellation boundary/reconciliation guidance, source fingerprint/idempotency, restart recovery, stable error categories, logs/DTO sanitization, and existing API publish idempotency. For every capability mark planned/covered, state the exact verifier or API artifact proving it, and record explicit opt-outs for browser OAuth/direct provider calls, richer title/hashtag authoring, Angular publish controls in Phase 8, automatic deletion/publication, post-public replacement/deletion, hashtags, artifact download, and final workflow-wide integration with the owning Phase 9/10/11 or future boundary. Cite D-01 through D-12 where applicable and state that Phase 8 accepts the basic title/summary start payload, returns sanitized privateWatchUrl, retains the API publish route, and hands richer metadata/publication UI to Phase 9/10.</action>
  <verify><automated>test -s .planning/phases/08-youtube-job-lifecycle/COVERAGE.md && rg -q 'YOUTUBE-01|YOUTUBE-02|Phase 9|Phase 10|Phase 11|D-01|D-12|public publish' .planning/phases/08-youtube-job-lifecycle/COVERAGE.md</automated></verify>
  <done>COVERAGE.md has a row for every provider/API capability and every opt-out has an explicit reason and owner; no capability is silently omitted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier fixtures -> API state machine | Fake provider inputs model hostile and partial external outcomes. |
| API logs -> operators/observability | Logging can accidentally serialize provider secrets or raw errors. |
| planning coverage -> later phases | Handoffs must not silently expand the Phase 8 public boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06 | Information disclosure | verifier/log assertions | high | mitigate | Add negative assertions for secrets, sessions, paths, payloads, stacks, raw reasons, and provider IDs. |
| T-08-07 | Denial/Tampering | fake provider retry scenarios | high | mitigate | Exercise bounded quota/timeout/network retries and same-row reconciliation after interruption. |
| T-08-08 | Repudiation | cancellation/reconciliation evidence | medium | mitigate | Assert durable cancellation timestamps/boundaries and private URL semantics in snapshots. |
| T-08-SC | Tampering | npm installs | high | mitigate | No package installation; use the existing verifier/build toolchain. |
</threat_model>

<verification>
Run the lifecycle verifier and inspect COVERAGE.md against all Phase 8 requirements, locked decisions, research constraints, and explicit later-phase opt-outs.
</verification>

<success_criteria>
The backend lifecycle has executable regression evidence for YOUTUBE-01 through YOUTUBE-05 and OPS-01 through OPS-04, plus a complete provider/API coverage record with explicit phase handoffs.
</success_criteria>

<output>
Create `.planning/phases/08-youtube-job-lifecycle/08-02-SUMMARY.md` when done.

## Artifacts this phase produces

- Executable backend lifecycle verifier covering metadata input, private URL output, publish idempotency, cancellation reconciliation, recovery, and security.
- Provider/API capability coverage matrix with explicit Phase 9/10/11 handoffs and opt-outs.
</output>
