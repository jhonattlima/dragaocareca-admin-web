---
phase: 10-operator-workflow-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/pages/manage/manage.component.ts
  - src/app/pages/manage/manage.component.spec.ts
autonomous: true
requirements: [OPS-05]
must_haves:
  truths:
    - "Opening an edited episode with persisted pending or processing transcript/summary work resumes exactly the applicable existing poller without starting duplicate polls."
    - "A restored completed or failed summary remains terminal, and a generated response never overwrites an operator-edited summary."
    - "Reload, edit switching, reset, replacement, and destroy retain the existing episode/job/source-generation stale-response protections."
  artifacts:
    - path: "src/app/pages/manage/manage.component.ts"
      provides: "Edit restoration helper that selects the existing transcript-to-summary polling path from persisted state."
      contains: "startEdit"
    - path: "src/app/pages/manage/manage.component.spec.ts"
      provides: "Focused regression coverage for pending/processing restoration, terminal states, manual summary preservation, and duplicate/stale guards."
  key_links:
    - from: "startEdit(episode)"
      to: "existing transcription/summary polling helpers"
      via: "restored episode status and editor identity/generation checks"
    - from: "summary polling response"
      to: "current editor formModel"
      via: "episode/editor identity guard plus summaryManuallyEdited protection"

---

<objective>
Repair the edit/reload seam so persisted summary work remains live and safe in the existing Manage workflow. Preserve the current polling implementation and generation guards; add only the restoration decision and regression coverage required by OPS-05.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/README.md
@docs/ARCHITECTURE.md
@docs/CONFIGURATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.planning/phases/10-operator-workflow-integration/10-RESEARCH.md
@.planning/phases/08-youtube-job-lifecycle/08-VERIFICATION.md
@.planning/phases/08-youtube-job-lifecycle/08-UAT.md
@.planning/phases/09-title-hashtag-authoring/09-VERIFICATION.md
@src/app/pages/manage/manage.component.ts
@src/app/pages/manage/manage.component.spec.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Resume the correct persisted transcript or summary stage on edit</name>
  <files>src/app/pages/manage/manage.component.ts, src/app/pages/manage/manage.component.spec.ts</files>
  <read_first>src/app/pages/manage/manage.component.ts, src/app/pages/manage/manage.component.spec.ts, .planning/phases/10-operator-workflow-integration/10-RESEARCH.md</read_first>
  <action>At the end of startEdit(), explicitly clear both existing transcript and summary pollers before choosing the restored stage, then route restored state through a named helper that chooses the existing transcription poll when transcriptStatus is pending/processing, otherwise starts the existing summary poll only when summaryStatus is pending/processing or suggested-tag work is non-terminal. Do not start both independent pollers for the same stage, do not create a second summary timer, and do not treat done/error/unavailable states as pollable. Preserve the existing editor identity/generation checks and summaryManuallyEdited behavior; generated summary text may update only when the operator has not edited it. Keep ApiService and backend contracts unchanged, and do not touch Phase 11 artifact selector/download behavior.</action>
  <verify><automated>npm test -- --watch=false --browsers=ChromeHeadless --include='src/app/pages/manage/manage.component.spec.ts'</automated></verify>
  <done>Editing an episode with pending persisted work clears any prior transcript and summary pollers, resumes the existing appropriate poll exactly once, and terminal states remain quiet; switching episodes cannot leave the prior episode’s poll active, and manual summary text survives all restored/generated responses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock reload, edit-switch, and stale summary regression coverage</name>
  <files>src/app/pages/manage/manage.component.spec.ts</files>
  <read_first>src/app/pages/manage/manage.component.spec.ts, src/app/pages/manage/manage.component.ts</read_first>
  <action>Add deferred-observable Jasmine coverage for pending and processing summary restoration, transcript-pending precedence, terminal no-poll behavior, one-poll/no-duplicate behavior after repeated edit/reload, manual summary preservation, reset/destroy teardown, and a late response from an older editor/episode being ignored. Assert that restoration clears both existing poller handles and that switching episodes cannot leave the prior transcript or summary poll active. Reuse the existing spies, fakeAsync utilities, and snapshot fixtures; assert the existing API method/episode IDs rather than implementation-private timer details where possible. Include a regression assertion that current YouTube job/source guards remain intact after edit restoration. Do not broaden tests into a new integration framework or introduce dependencies.</action>
  <verify><automated>./node_modules/.bin/tsc -p tsconfig.spec.json --noEmit && npm test -- --watch=false --browsers=ChromeHeadless --include='src/app/pages/manage/manage.component.spec.ts'</automated></verify>
  <done>Focused Manage tests fail if edit restoration fails to clear prior pollers, duplicates polling, overwrites manual text, lets stale responses mutate the current editor, or breaks existing YouTube recovery guards.</done>
</task>
</tasks>

<verification>
Run the focused Manage suite and spec compilation. The live browser reload checkpoint is covered by Plan 03 because unit tests cannot prove navigation persistence against a running API.
</verification>

<success_criteria>
Persisted summary/transcript work is usable after edit/reload, existing generation and manual-edit protections remain authoritative, and no provider or artifact business logic enters Angular.
</success_criteria>

<output>Create `.planning/phases/10-operator-workflow-integration/10-01-SUMMARY.md` when done.</output>
