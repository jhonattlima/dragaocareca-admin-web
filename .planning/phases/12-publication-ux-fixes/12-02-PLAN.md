# Phase 12 Plan 02: Trailer Preview and Form Layout

## Objective

Correct the generated trailer title preview and rebalance the first episode identity row while retaining the current sectioned responsive form.

## Requirements

- TITLE-01, TITLE-02
- LAYOUT-01, LAYOUT-02
- VERIFY-01, VERIFY-02, VERIFY-03

## Tasks

### 1. Fix trailer title composition

Update the preview prefix to include the episode number in the exact operator-facing format `Trailer - DC <episode number> - <episode name>`. Use the same assembled value sent to the API/YouTube commit so preview and publication cannot diverge. Cover missing/invalid number behavior with the existing validation boundary and retain Unicode-aware 100-character validation.

### 2. Rebalance the identity row

Adjust the Bootstrap column classes in `episode-form.component.html` so Episode # and Episode Type are narrower and Title receives the released width. Keep the row responsive and do not alter unrelated fields or the legacy-inspired section layout.

### 3. Update focused UI coverage and documentation

Update title expectations and add template/component coverage for the `DC` number prefix and identity-row classes where the existing test setup supports it. Update canonical docs if the visible publication/reset or trailer-title contract changes. Run the required build and focused test commands.

## Verification

- Trailer title tests assert the episode number, title, hashtags, and Unicode length.
- Focused frontend tests pass.
- `npm run build` passes.
- Local happy-path publication confirms the popup, clean upload cards, correct preview, and no false cancellation message.

## Files Expected

- `src/app/pages/manage/manage.component.ts`
- `src/app/pages/manage/episode-form.component.html`
- `src/app/pages/manage/manage.component.spec.ts`
- `docs/README.md`, `docs/ARCHITECTURE.md`, or `docs/CONFIGURATION.md` only if contract wording needs reconciliation
