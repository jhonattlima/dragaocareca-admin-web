---
phase: 08.1-episode-form-stability-patch-fix-add-episode-defaults-audio
plan: 05
type: execute
wave: 2
depends_on: [08.1-04]
files_modified:
  - src/app/pages/manage/episode-form.component.html
  - src/app/pages/manage/episode-form.component.scss
  - src/app/pages/manage/manage.component.spec.ts
autonomous: false
gap_closure: true
gap_ids: [G-08.1-2, G-08.1-4]
requirements: [FORM-05]
user_setup: []
must_haves:
  truths:
    - "Duration and Bytes remain native readonly controls and have a strong, perceptible light-gray presentation distinct from editable controls."
    - "Duration and Bytes have explicit readonly border, text, cursor, and focus-state styling that does not suggest they are editable."
    - "The fix adds no helper or hint text and does not change Spotify ID markup or scope it into this gap closure."
  artifacts:
    - "The episode form template gives Duration and Bytes a dedicated readonly metadata class while retaining native readonly attributes."
    - "The episode form stylesheet defines the dedicated class with light-gray background, border, text, cursor, and focus rules."
    - "Focused form assertions cover the two target controls’ readonly/class contract and the absence of hint copy."
  key_links:
    - "Duration and Bytes template class names match the scoped selectors in episode-form.component.scss."
    - "The dedicated styling overrides Bootstrap form-control focus/border affordances without changing audio metadata mapping or form values."
---

<objective>
Close G-08.1-2 and G-08.1-4 by making Duration and Bytes visibly readonly with a perceptible light-gray treatment and explicit interaction styling, while leaving Spotify ID outside this fix.

Purpose: Native readonly semantics are already present, but the current tint is too subtle and Bootstrap retains editable-looking border/focus affordances. The visual boundary must communicate non-editability without helper text or behavior changes, per D-12 and D-13.
Output: Scoped template/CSS styling for Duration and Bytes, focused assertions, build/test evidence, and a human visual verification checkpoint.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-UAT.md
@.planning/debug/g-08-1-4-readonly-fields.md
@.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-02-SUMMARY.md
@src/app/pages/manage/episode-form.component.html
@src/app/pages/manage/episode-form.component.scss
@src/app/pages/manage/manage.component.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scoped readonly presentation for Duration and Bytes</name>
  <files>src/app/pages/manage/episode-form.component.html, src/app/pages/manage/episode-form.component.scss, src/app/pages/manage/manage.component.spec.ts</files>
  <behavior>
    - Test 1: Duration and Bytes are native readonly text controls with the dedicated readonly metadata class.
    - Test 2: The template contains no helper/hint text introduced for these fields, and Spotify ID is not changed as part of the target contract.
    - Test 3: The stylesheet contract defines a perceptible light-gray background plus explicit border, text, cursor, and focus behavior for the dedicated class.
  </behavior>
  <action>Give the Duration and Bytes inputs in `episode-form.component.html` a dedicated class in addition to their existing `form-control` and native `readonly` attributes; do not change the Spotify ID control or add hint/helper copy. In `episode-form.component.scss`, target the dedicated class with sufficient selector specificity for Bootstrap and define a clearly perceptible light-gray background, muted readable text, an explicit muted border, a non-editing cursor, and focus rules that preserve a visible readonly boundary without the normal editable focus treatment. Keep the existing `[(ngModel)]` Duration binding, `[value]` formatted Bytes binding, raw bytes state, and upload behavior unchanged. Extend the focused frontend spec with deterministic template/style contract assertions for the two target fields and no hint text, implementing D-12/D-13 and closing G-08.1-2/G-08.1-4.</action>
  <verify>
    <automated>npx tsc -p tsconfig.spec.json --noEmit</automated>
    <automated>npm test -- --watch=false --browsers=ChromeHeadless</automated>
  </verify>
  <done>Duration and Bytes have the dedicated scoped class, native readonly attributes, perceptible light-gray/border/text/cursor/focus styling, no added hint text, and focused assertions that fail if the styling contract or target scope regresses.</done>
</task>

<task type="auto">
  <name>Task 2: Run frontend build and focused regression gates</name>
  <files>src/app/pages/manage/episode-form.component.html, src/app/pages/manage/episode-form.component.scss, src/app/pages/manage/manage.component.spec.ts</files>
  <action>Run the focused Angular suite and production build after the scoped readonly change. Confirm the existing audio metadata response mapping, decimal-MB presentation, trailer-audio mapping, participant tests from Plan 04, and Spotify ID markup remain intact. Treat a real test/compiler/build failure as blocking; preserve the documented runner-unavailable classification if ChromeHeadless cannot launch.</action>
  <verify>
    <automated>npm test -- --watch=false --browsers=ChromeHeadless</automated>
    <automated>npm run build</automated>
  </verify>
  <done>All available focused tests and the production build pass, with no regression to audio/trailer behavior or participant defaults and no production API changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human-verify readonly visual treatment</name>
  <files>src/app/pages/manage/episode-form.component.html, src/app/pages/manage/episode-form.component.scss</files>
  <action>After the automated gates pass, inspect the rendered Manage/Add Episode form at the target fields and confirm the perceptible readonly treatment described below without expanding scope to Spotify ID.</action>
  <what-built>Duration and Bytes now use a dedicated readonly presentation with a perceptible light-gray background, muted text, explicit border, non-editing cursor, and readonly focus behavior. Spotify ID remains outside the targeted markup/style change, and no hint text was added.</what-built>
  <how-to-verify>
    1. Start the frontend with `npm start` and open the Manage/Add Episode form.
    2. Locate Duration and Bytes in the File Management metadata row; confirm both are visibly light gray and visually distinct from editable fields.
    3. Click or tab to each field; confirm it cannot be edited, uses a non-editing cursor, and does not show an editable-style focus ring while retaining a clear boundary.
    4. Confirm no helper or hint text appears for either field and do not evaluate Spotify ID as part of this gap closure.
  </how-to-verify>
  <verify>
    <automated>npm run build</automated>
    <human-check>Rendered Duration and Bytes have the approved perceptible readonly treatment and no added hint text.</human-check>
  </verify>
  <done>Human verification approves the rendered Duration and Bytes treatment, with Spotify ID outside the scope of this checkpoint.</done>
  <resume-signal>Type "approved" or describe any visual/accessibility issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser operator → readonly form controls | User interaction reaches metadata fields that must remain non-editable and display truthful state. |
| Template → component state/API payload | Form bindings display confirmed metadata; styling must not alter raw values or upload contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.1-05-01 | Tampering | Duration/Bytes inputs | medium | mitigate | Retain native readonly attributes, test the DOM contract, and visually verify that styling does not imply editability. |
| T-08.1-05-02 | Information disclosure | Episode form template | low | accept | These fields display already-authorized episode metadata; the plan changes presentation only and does not add data exposure. |
| T-08.1-05-SC | Tampering | npm/pip/cargo installs | high | accept | No package installation is required by this plan; existing dependencies are not changed. |
</threat_model>

<verification>
Run `npx tsc -p tsconfig.spec.json --noEmit`, `npm test -- --watch=false --browsers=ChromeHeadless`, and `npm run build`, then complete the blocking browser visual checkpoint. Confirm Duration and Bytes styling is perceptible and scoped, no hint text exists, Spotify ID remains outside the fix, and existing audio/trailer/participant tests remain covered.
</verification>

<success_criteria>
G-08.1-2 and G-08.1-4 are closed when Duration and Bytes are visibly readonly with the specified styling affordances and no hint text, focused automated coverage and frontend build pass, and human verification approves the rendered result.
</success_criteria>

<output>
Create `.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-05-SUMMARY.md` when done.
</output>
