---
phase: 08.1-episode-form-stability-patch-fix-add-episode-defaults-audio
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/environments/environment.ts
  - src/environments/environment.staging.ts
  - src/environments/environment.prod.ts
  - src/app/pages/manage/manage.component.spec.ts
autonomous: true
gap_closure: true
gap_ids: [G-08.1-3]
requirements: [FORM-03]
user_setup: []
must_haves:
  truths:
    - "Development, staging, and production configure the four required participant names in the required order."
    - "Opening a new episode selects configured names that exist in memberOptions, while unknown configured names remain excluded."
    - "Editing an existing episode preserves its current selected participants."
  artifacts:
    - "All three frontend environment files expose the same four-name defaultParticipants array."
    - "The focused ManageComponent spec asserts the shipped environment values and the existing intersection/edit-preservation behavior."
  key_links:
    - "Each Angular build replacement resolves to a four-name defaultParticipants value consumed by ManageComponent.configuredParticipantNames."
    - "applyConfiguredParticipantDefaults continues to intersect configuration with memberOptions and remains gated for new episodes."
---

<objective>
Close G-08.1-3 by shipping the exact four configured participant defaults in every frontend environment and locking that configuration plus the existing selection behavior with a focused assertion.

Purpose: The diagnosed mismatch is configuration data, not participant-selection logic. This restores the UAT default selection while preserving catalog intersection and edit-flow semantics per D-09 and D-10.
Output: Updated development/staging/production participant arrays and regression coverage in the existing ManageComponent focused spec.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-UAT.md
@.planning/debug/g08-1-3-default-participants.md
@.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-02-SUMMARY.md
@src/app/pages/manage/manage.component.ts
@src/app/pages/manage/manage.component.spec.ts
@src/environments/environment.ts
@src/environments/environment.staging.ts
@src/environments/environment.prod.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the shipped participant configuration and selection contract</name>
  <files>src/app/pages/manage/manage.component.spec.ts, src/environments/environment.ts, src/environments/environment.staging.ts, src/environments/environment.prod.ts</files>
  <behavior>
    - Test 1: The development, staging, and production environment exports each contain `defaultParticipants` equal to `['Jhonatt Lima', 'Diego Broniszak', 'Eric Farias', 'Gabriel Moraes']`.
    - Test 2: Applying the shipped defaults to a new editor selects all four names because each is present in `memberOptions`.
    - Test 3: An unknown configured name is filtered out and an editor with `editingEpisodeId` retains its existing selection.
  </behavior>
  <action>Update `defaultParticipants` in all three environment files to exactly `Jhonatt Lima`, `Diego Broniszak`, `Eric Farias`, and `Gabriel Moraes`, in that order, without changing `production`, `authBypass`, API URLs, or any other environment key. Extend `src/app/pages/manage/manage.component.spec.ts` with imports for all three environment exports and a focused assertion for the shipped arrays, then exercise the existing `applyConfiguredParticipantDefaults` seam with the real four-name configuration and a separate unknown-name/edit-preservation case. Keep the existing memberOptions catalog and ManageComponent filtering implementation unchanged; this implements D-09 and D-10 and closes G-08.1-3.</action>
  <verify>
    <automated>npx tsc -p tsconfig.spec.json --noEmit</automated>
    <automated>npm test -- --watch=false --browsers=ChromeHeadless</automated>
  </verify>
  <done>The three environment arrays exactly match the required four-name order; focused assertions prove all four known defaults select for a new episode, unknown names are ignored, and active edits are not overwritten.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the participant gap closure through the production build</name>
  <files>src/environments/environment.ts, src/environments/environment.staging.ts, src/environments/environment.prod.ts, src/app/pages/manage/manage.component.spec.ts</files>
  <action>Run the focused participant assertions and the production build after Task 1. Confirm Angular’s development, staging, and production file replacements still resolve valid environment objects and that no authBypass or API configuration changed as a side effect. Record any missing ChromeHeadless binary as runner-unavailable while treating TypeScript/build failures as blockers.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm test -- --watch=false --browsers=ChromeHeadless</automated>
  </verify>
  <done>The frontend production build passes, the participant-focused suite has no assertion or compilation failures, and the gap-closure evidence covers all three build environments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build configuration → browser bundle | Environment values become client-visible participant defaults and must not alter auth/API settings. |
| ManageComponent state → episode write | Selected participant names are user-editable form data and remain subject to existing API validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.1-04-01 | Tampering | `src/environments/environment*.ts` | medium | mitigate | Assert all three shipped arrays exactly and build through Angular’s environment replacements without changing authBypass or API keys. |
| T-08.1-04-02 | Elevation of privilege | `applyConfiguredParticipantDefaults` | low | mitigate | Preserve catalog intersection and active-edit guard; focused tests prove unknown names cannot enter selection and edits are not overwritten. |
| T-08.1-04-SC | Tampering | npm/pip/cargo installs | high | accept | No package installation is required by this plan; existing dependencies are not changed. |
</threat_model>

<verification>
Run `npx tsc -p tsconfig.spec.json --noEmit`, `npm test -- --watch=false --browsers=ChromeHeadless`, and `npm run build`. Confirm the exact four-name arrays in development, staging, and production and preserve the existing explicit browser-runner limitation if ChromeHeadless is unavailable.
</verification>

<success_criteria>
G-08.1-3 is closed when all three Angular environment replacements ship the exact four names in order, the focused test proves known-name selection plus unknown-name filtering and edit preservation, and the frontend build passes.
</success_criteria>

<output>
Create `.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-04-SUMMARY.md` when done.
</output>
