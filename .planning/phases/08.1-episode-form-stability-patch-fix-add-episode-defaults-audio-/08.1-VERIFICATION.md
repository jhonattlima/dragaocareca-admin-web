---
phase: 08.1-episode-form-stability-patch-fix-add-episode-defaults-audio
verified: 2026-08-14T04:58:00Z
status: human_needed
score: 0/5 must-haves verified
behavior_unverified: 5
overrides_applied: 0
behavior_unverified_items:
  - truth: "Add Episode publication defaults use the latest valid publication date/time plus seven calendar days, or current local date/time when there is no prior valid episode."
    test: "Run the focused FORM-01 Jasmine specs in a working ChromeHeadless/Karma environment and exercise latest, empty, invalid, and local-clock cases."
    expected: "The Add Episode datetime-local value is latest publication plus seven calendar days with its local clock preserved, or current local date/time for empty/invalid input."
    why_human: "The focused spec exists and TypeScript compiles, but Karma could not bind port 9876, so no browser assertion passed in this verification run."
  - truth: "Episode-audio upload uses backend-confirmed HH:MM:SS duration and nonnegative bytes, fails closed on missing metadata, and leaves trailer-audio behavior unchanged."
    test: "Run the focused FORM-02 Angular specs plus the sibling API contract verifier against a writable API build/runtime."
    expected: "Only a successful episode-audio response with confirmed metadata updates the form; metadata failure is reported; trailer-audio remains filename/message-only."
    why_human: "The API source, compiled contract verifier, and frontend tests are present, but the Angular behavioral assertions did not run and the API build could not rewrite read-only dist/."
  - truth: "Configured participant defaults select only names present in the loaded participant catalog."
    test: "Run the focused FORM-03 Jasmine spec in ChromeHeadless."
    expected: "Known configured names are selected, unknown names are ignored, and active edits are not overwritten."
    why_human: "This is a state transition covered by a focused spec, but the browser runner was unavailable."
  - truth: "Save and API writes require at least one complete music credit with a non-empty name and reference link."
    test: "Run the focused FORM-04 Angular spec and the sibling API verifier."
    expected: "Partial credits keep Save disabled and are rejected by the API; a trimmed name plus non-empty reference URL is accepted."
    why_human: "The API verifier passed, but the Angular Save-gating assertion could not execute because Karma could not bind its port."
  - truth: "Duration, Bytes, and Spotify ID are read-only light-gray fields without hint text."
    test: "Render the Add Episode form in a working browser and inspect the three controls."
    expected: "All three controls are native readonly controls with the shared light-gray styling and no helper/hint copy."
    why_human: "DOM/rendering and visual styling cannot be fully certified by source inspection alone; the Angular browser suite was unavailable."
human_verification:
  - test: "Run the focused Angular/Karma suite for the FORM-01 through FORM-05 specs using a ChromeHeadless binary and a permitted Karma port."
    expected: "All focused FORM specs pass, including date defaults, audio mapping/fail-closed behavior, participant filtering, music Save gating, decimal-MB formatting, trailer compatibility, and readonly styling."
    why_human: "The current environment reports `listen EPERM` while binding Karma port 9876, so no Jasmine assertions ran."
  - test: "Run the sibling API build and episode-audio contract verifier from a writable API checkout, then exercise persistence/reload through the API boundary."
    expected: "The API build succeeds and the confirmed duration/bytes and complete music-credit data survive create/update and subsequent list/edit reload."
    why_human: "The sibling checkout's dist/ is read-only here; source typecheck passes and the existing compiled verifier passes, but a fresh build/runtime persistence run was not possible."
---

# Phase 8.1: Episode Form Stability Patch Verification Report

**Phase Goal:** Make Add Episode defaults, episode-audio metadata, participant/music validation, and read-only field presentation reliable without changing trailer-audio behavior.
**Verified:** 2026-08-14T04:58:00Z
**Status:** PARTIAL / human-needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | Add Episode publication defaults use latest valid publication date/time plus seven calendar days, or current local date/time when no prior valid episode exists. | PRESENT_BEHAVIOR_UNVERIFIED | `manage.component.ts:2737-2749` filters invalid dates, selects the latest, uses `setDate(+7)`, and falls back to current local time. Focused FORM-01 specs exist and compile, but did not execute. |
| 2 | Episode-audio upload receives backend-confirmed duration/bytes, fails closed on missing metadata, and does not change trailer-audio behavior. | PRESENT_BEHAVIOR_UNVERIFIED | API route calls `extractEpisodeAudioMetadata` only for `audio` (`../dragaocareca-admin-api/src/routes/episodes.routes.ts:295-299`), returns/persists metadata (`:312-323`, `:335-359`), and cleans up on failure (`:368-372`). Angular validates/maps only audio (`manage.component.ts:2540-2574`). Trailer path remains separate. API compiled verifier passed; Angular browser assertions did not execute. |
| 3 | Configured participant defaults select only catalog names. | PRESENT_BEHAVIOR_UNVERIFIED | `environment*.ts` define `defaultParticipants`; `manage.component.ts:1852-1859` intersects with `memberOptions` and skips active edits. Focused FORM-03 spec exists and compiles, but did not execute. |
| 4 | Save and API writes require one complete music credit. | PRESENT_BEHAVIOR_UNVERIFIED | Angular Save gate and serializer are at `manage.component.ts:1366-1377` and `1889-1912`; API predicate/schema are `../dragaocare-admin-api/src/schemas/episode.ts:26-40,43-68`. API contract verifier passed; Angular browser assertion did not execute. |
| 5 | Duration, Bytes, and Spotify ID are readonly light-gray fields without hint text. | PRESENT_BEHAVIOR_UNVERIFIED | Template uses `readonly-field` on all three controls (`episode-form.component.html:154-198`); style is light gray at `episode-form.component.scss:8-11`. Browser rendering was unavailable. |

**Score:** 0/5 truths behavior-verified (5 present and wired, behavior-unverified)

## Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/app/pages/manage/manage.component.ts` | Defaults, upload mapping, participant filtering, Save validation, payload persistence | VERIFIED | Substantive and used by `manage.component.html`; build and spec TypeScript compilation pass. |
| `src/app/pages/manage/episode-form.component.html` | Form fields, music validation feedback, readonly presentation | VERIFIED | Substantive template used by `ManageComponent`; all three readonly fields are present. |
| `src/app/pages/manage/episode-form.component.scss` | Shared light-gray readonly styling | VERIFIED | `.readonly-field` is defined and applied. |
| `src/environments/environment.ts`, `.prod.ts`, `.staging.ts` | Configurable participant defaults while preserving authBypass | VERIFIED | All three define `defaultParticipants`; authBypass remains true in development and false in staging/production. |
| `../dragaocareca-admin-api/src/services/episode-audio-metadata.service.ts` | Bounded backend duration/bytes confirmation | VERIFIED | Uses `ffprobe`, file stat bytes, validation, and HH:MM:SS normalization. |
| `../dragaocareca-admin-api/src/routes/episodes.routes.ts` | Audio-only integration and fail-closed cleanup | VERIFIED | Source wiring is present; API source typecheck passes. |
| `../dragaocareca-admin-api/src/database/repositories/episode.repository.ts` | Persist/reload duration and bytes | VERIFIED | `updateMedia`, insert/update SQL, and row mapping include both fields. |
| `../dragaocareca-admin-api/src/schemas/episode.ts` | Authoritative complete music-credit validation | VERIFIED | Shared predicate is applied to episode writes. |
| `docs/README.md`, `docs/ARCHITECTURE.md`, `docs/CONFIGURATION.md`, `docs/TESTING.md` | Synchronized contract/auth/testing docs | VERIFIED | Docs describe FORM-01..05, authBypass, trailer compatibility, and runner limitation. |

## Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| Add Episode form | `ManageComponent` | Angular template bindings | WIRED | Upload controls, readonly fields, participants, music credits, and Save gate bind to component state. |
| `ManageComponent.uploadMedia` | `ApiService.uploadEpisodeAudio` / trailer wrapper | kind-specific request selection and `HttpResponse` mapping | WIRED | Audio requires API metadata and maps raw duration/bytes; trailer maps filename/message only. |
| API audio route | metadata service | `spec.kind === "audio" ? extractEpisodeAudioMetadata(...) : null` | WIRED | Trailer route does not invoke metadata extraction. |
| API audio route | episode repository | `updateMedia` with duration/bytes; row response | WIRED | Existing episodes persist confirmed fields; create payload preserves draft response values for subsequent save. |
| Angular upload response | create/update API write | `buildPayload` includes raw `duration` and `bytes` | WIRED | `manage.component.ts:1889-1912`; reload uses API DTO fields in `startEdit`. |

## Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| Add Episode form | `duration`, `bytes` | API `ffprobe` plus staged-file stat | Yes, source-wired | FLOWING (runtime browser/API integration not executed here) |
| Add Episode form | `selectedMembers` | `environment.defaultParticipants` intersected with `memberOptions` | Yes | FLOWING |
| Episode edit form | `duration`, `bytes`, `musicCredits` | API repository row mapping/list response | Yes | FLOWING |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Frontend production build | `npm run build` | Exit 0; only pre-existing selector/budget warnings | PASS |
| Frontend spec TypeScript compilation | `npx tsc -p tsconfig.spec.json --noEmit` | Exit 0 | PASS |
| Angular focused browser assertions | `npm test -- --watch=false --browsers=ChromeHeadless` | Exit 1; Karma could not bind `0.0.0.0:9876` (`listen EPERM`) | LIMITED |
| Sibling API source typecheck | `npm run typecheck` in `../dragaocareca-admin-api` | Exit 0 | PASS |
| Sibling API compiled episode-audio verifier | `NODE_ENV=development DISABLE_BACKGROUND_WORKERS=true node dist/scripts/verify-episode-audio-contract.js` | Exit 0; `Episode audio contract verification passed.` | PASS |
| Sibling API fresh build | `npm run build` in `../dragaocareca-admin-api` | Exit 2 because sandbox cannot write sibling `dist/` (`EROFS`); no TypeScript diagnostic | LIMITED |

## Probe Execution

No phase-declared or conventional `scripts/*/tests/probe-*.sh` probe was found.

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| FORM-01 | 08.1-00..03 | Latest/current publication defaults | NEEDS HUMAN | Source and focused spec present; browser assertion unavailable. |
| FORM-02 | 08.1-00..03 | Confirmed episode-audio metadata, fail-closed behavior, trailer compatibility | NEEDS HUMAN | Source wiring, API typecheck, and compiled verifier pass; fresh browser/API runtime gate unavailable. |
| FORM-03 | 08.1-00..03 | Configured participant/catalog intersection | NEEDS HUMAN | Source and focused spec present; browser assertion unavailable. |
| FORM-04 | 08.1-00..03 | Complete music credit required in UI/API | NEEDS HUMAN | API verifier passes; UI assertion unavailable. |
| FORM-05 | 08.1-00..03 | Readonly/light-gray metadata fields | NEEDS HUMAN | Template/style source present; browser rendering unavailable. |

No additional Phase 8.1 requirement mapped in `REQUIREMENTS.md` was orphaned.

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| Phase source files | — | No unreferenced `TODO`, `FIXME`, `TBD`, `XXX`, placeholder, or empty implementation found in the inspected Phase 8.1 implementation paths. | Info | No blocker found. |

## Human Verification Required

1. Run the focused Angular/Karma suite in an environment with ChromeHeadless and a permitted Karma port; all FORM-01 through FORM-05 specs must pass.
2. Run a fresh writable sibling API build and exercise audio upload → create/update → list/edit reload, including metadata failure cleanup and complete-credit rejection.
3. Inspect the rendered form to confirm readonly styling and absence of helper/hint text.

## Gaps Summary

The Add Episode patch is implemented and source-wired across the frontend and sibling API. The API verifier and both source/build-adjacent type checks pass, and the frontend production build passes. However, this verification cannot certify runtime behavior because Karma cannot bind port 9876, and the sibling API build cannot rewrite its read-only `dist/` directory. The phase therefore remains PARTIAL / human-needed under the verification gate; no production code was modified.

---

_Verified: 2026-08-14T04:58:00Z_
_Verifier: the agent (gsd-verifier)_
