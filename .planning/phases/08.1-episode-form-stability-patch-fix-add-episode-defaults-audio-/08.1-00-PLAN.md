---
phase: 08.1-episode-form-stability-patch-fix-add-episode-defaults-audio
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/app/pages/manage/manage.component.spec.ts
  - src/app/core/api.service.spec.ts
  - ../dragaocareca-admin-api/src/scripts/verify-episode-audio-contract.ts
autonomous: true
requirements: [FORM-01, FORM-02, FORM-03, FORM-04, FORM-05]
must_haves:
  truths:
    - "RED Angular coverage exists for every FORM-01 through FORM-05 behavior before implementation begins."
    - "RED API contract coverage proves backend-confirmed audio metadata, complete music credits, draft persistence/cleanup, and trailer-audio compatibility are required."
    - "The fixed two-decimal decimal-MB presentation rule is asserted at exact and fractional byte boundaries."
  artifacts:
    - "Focused ManageComponent/API Jasmine specs covering FORM-01 through FORM-05."
    - "A sibling API audio-contract verifier covering success and failure paths."
  key_links:
    - "Each FORM requirement maps to a named failing assertion that later implementation plans must turn green."

---

<objective>
Establish executable Wave 0 RED coverage for all Phase 8.1 form requirements before any implementation plan runs.

Purpose: Make the phase's Nyquist coverage a scheduled dependency rather than prose embedded in later implementation tasks.
Output: Failing Angular/API tests that define the locked contracts, including D-01 through D-13 and the fixed decimal-MB rule.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-CONTEXT.md
@.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-RESEARCH.md
@docs/README.md
@docs/ARCHITECTURE.md
@docs/CONFIGURATION.md
@docs/TESTING.md
@src/app/pages/manage/manage.component.ts
@src/app/core/api.service.ts
@src/app/pages/manage/episode-form.component.html
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Scaffold RED Angular coverage for FORM-01 through FORM-05</name>
  <files>src/app/pages/manage/manage.component.spec.ts, src/app/core/api.service.spec.ts</files>
  <behavior>
    - FORM-01: latest valid publication date plus seven calendar days preserves local clock/timezone representation; empty and invalid lists use the current local datetime safely.
    - FORM-02: completed episode-audio responses require backend duration and bytes, retain raw confirmed bytes, reject missing metadata, and leave trailer-audio mapping filename-only.
    - FORM-03: configured participant names select only matching current catalog entries and never overwrite an active edit.
    - FORM-04: a music credit requires trimmed name plus at least one trimmed non-empty reference URL, with partial entries rejected.
    - FORM-05: Duration, Bytes, and Spotify ID are native readonly/light-gray fields without helper hints.
    - D-07 precision: decimal MB divides by 1,000,000 and rounds nonnegative byte counts half-up to exactly two decimal places, including 0, 1,000,000, 1,234,567, and 1,235,000 bytes.
  </behavior>
  <action>Create or extend the existing Angular focused specs with deterministic fixtures and mocked HTTP events. Assert the locked decisions D-01 through D-13 and the resolved fixed two-decimal decimal-MB rule. Do not use browser File.size as authoritative metadata. Keep the tests intentionally RED against the current implementation; do not weaken unrelated lifecycle coverage or replace the later implementation task's production seams.</action>
  <verify><automated>npm test -- --watch=false --browsers=ChromeHeadless</automated></verify>
  <done>Angular specs exist on disk with named assertions covering every FORM-01 through FORM-05 requirement and the fixed precision rule; the run is either an assertion failure (expected RED) or an explicitly recorded ChromeHeadless runner-unavailable limitation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold RED sibling API contract coverage</name>
  <files>/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/scripts/verify-episode-audio-contract.ts</files>
  <behavior>
    - Backend-confirmed episode-audio duration is HH:MM:SS and bytes are nonnegative integers; missing or unusable metadata rejects success and cleans failed staging.
    - Draft upload metadata flows through Angular payload shape to POST/create persistence and reload, with create-failure cleanup retaining only the documented retryable staged source.
    - Incomplete music credits fail both create and update while a trimmed complete credit passes.
    - Trailer-audio responses preserve the existing filename/message contract.
  </behavior>
  <action>Create the repeatable API verifier using the sibling API's existing script/assertion conventions and no new dependency. Keep it RED until Plan 01 supplies the API implementation. Cite FORM-02 and FORM-04 plus D-04, D-05, D-08, and D-11 in the assertions or test descriptions. Do not install packages.</action>
  <verify><automated>cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api && npm run build && NODE_ENV=development DISABLE_BACKGROUND_WORKERS=true node dist/scripts/verify-episode-audio-contract.js</automated></verify>
  <done>The sibling API verifier is a scheduled Wave 0 artifact that fails on the current missing contract and names the success, failure-cleanup, persistence, validation, and trailer non-regression cases required by later plans.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Angular test fixtures -> component contract | Fixtures must not accidentally make browser-derived metadata authoritative. |
| API verifier -> upload/write seams | Test inputs cross the same untrusted upload and episode-write boundaries as production requests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08.1-00-01 | Tampering | RED audio metadata tests | medium | mitigate | Assert backend-confirmed duration/bytes and reject missing fields before implementation is accepted. |
| T-08.1-00-02 | Tampering | RED music-credit tests | medium | mitigate | Assert the same trimmed complete-credit invariant required at API writes and the UI gate. |
| T-08.1-SC | Tampering | npm/pip/cargo installs | low | accept | No package installation is planned; use the existing test/build toolchain. |
</threat_model>

<verification>
Confirm both test artifacts exist before Wave 1, each FORM-01 through FORM-05 requirement has a named RED assertion, and the RED runs distinguish assertion failures from an unavailable ChromeHeadless runner.
</verification>

<success_criteria>
Wave 0 is complete only when the phase has executable pre-implementation tests for all five FORM requirements and the fixed precision rule, with implementation plans depending on 08.1-00.
</success_criteria>

<output>
Create `.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-00-SUMMARY.md` when done.
</output>
