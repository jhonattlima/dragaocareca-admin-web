---
phase: 08.1-episode-form-stability-patch-fix-add-episode-defaults-audio
plan: 03
type: execute
wave: 3
depends_on: [08.1-02]
files_modified:
  - docs/README.md
  - docs/ARCHITECTURE.md
  - docs/CONFIGURATION.md
  - docs/TESTING.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-VALIDATION.md
autonomous: true
requirements: [FORM-01, FORM-02, FORM-03, FORM-04, FORM-05]
must_haves:
  truths:
    - "Canonical frontend and API documentation describe the same audio metadata, participant configuration, music-credit, and read-only contracts."
    - "The phase passes frontend tests/build and the sibling API build/contract verification without weakening authBypass or trailer-audio compatibility."
    - "Roadmap and requirements traceability identify FORM-01 through FORM-05 as implemented by Phase 8.1 plans."
  artifacts:
    - "Updated docs explain the environment participant key and backend-confirmed audio response fields."
    - "Testing documentation lists the focused Angular and API verification commands and ChromeHeadless limitation handling."
    - "Roadmap/requirements metadata records the four executable plans and Phase 8.1 coverage."
  key_links:
    - "Docs examples match the actual ApiService Episode DTO and sibling API OpenAPI response."
    - "Final verification runs the complete API draft-upload response -> Angular state -> buildPayload -> POST /episodes -> persisted DTO -> reload path, including upload/probe and create/cleanup failure behavior, while checking trailer-audio compatibility explicitly and recording the result in 08.1-VALIDATION.md."
---

<objective>
Close the phase with synchronized documentation, traceability, and cross-repository compatibility verification.

Purpose: The repository's source-of-truth docs and future executors must understand the contract without rediscovering the implementation.
Output: Updated canonical docs, requirement traceability, and recorded green/limited verification evidence.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-CONTEXT.md
@.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-RESEARCH.md
@docs/README.md
@docs/ARCHITECTURE.md
@docs/CONFIGURATION.md
@docs/TESTING.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Synchronize canonical documentation and requirement traceability</name>
  <files>docs/README.md, docs/ARCHITECTURE.md, docs/CONFIGURATION.md, docs/TESTING.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>Document the final API/UI contract: publication default algorithm and local-time preservation; audio-only backend-confirmed duration/bytes with HH:MM:SS and decimal-MB display fixed to two decimal places using deterministic nonnegative half-up rounding plus fail-closed metadata errors; editable environment participant defaults intersected with the member catalog; complete music-credit Save/API validation; and read-only light-gray Duration/Bytes/Spotify ID with no hints. Preserve the existing authBypass explanation, sectioned layout guidance, and trailer-video documentation. Update Testing with exact Angular and sibling API commands and describe ChromeHeadless-unavailable evidence. Update Phase 8.1 plan count/list and FORM-01 through FORM-05 traceability using scoped edits only; do not rewrite unrelated roadmap phases.</action>
  <verify><automated>rg -n "FORM-01|FORM-02|FORM-03|FORM-04|FORM-05|participant|duration|bytes|music credit|authBypass" docs/README.md docs/ARCHITECTURE.md docs/CONFIGURATION.md docs/TESTING.md .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated></verify>
  <done>All canonical docs and planning traceability agree with the shipped contract and preserve unrelated phase entries and existing auth/trailer assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Run final gates and update the validation ledger</name>
  <files>.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-VALIDATION.md</files>
  <action>Run the browser suite, frontend build, sibling API build, and API contract verifier as independent gates, capturing each exit status and output. Classify a nonzero Angular status as runner-unavailable only when the output explicitly matches a missing ChromeHeadless/Chrome binary or launcher; any real Jasmine/Karma assertion or compilation failure remains a failed gate and must fail the overall verification. When the runner is unavailable, continue to the frontend build and both API commands and record the exact limitation, but do not mark browser tests passed or convert build success into test success. The API verifier must assert the complete draft-upload response through Angular payload contract, POST/create persisted DTO, reload equivalence, and upload/probe/create cleanup paths, plus trailer-audio compatibility. Update 08.1-VALIDATION.md from its planned initial state with status, FORM-01 through FORM-05 sign-off, per-command evidence, browser-runner classification, and final status (`passed` only when Angular tests pass; `limited` when only the runner is unavailable and all other gates pass; `failed` for any real test/build/API failure). Inspect git diff/status in both repositories and preserve unrelated working-tree changes.</action>
  <verify><automated>bash -lc 'set +e; npm test -- --watch=false --browsers=ChromeHeadless &gt; /tmp/phase-08-1-angular.log 2&gt;&amp;1; angular=$?; npm run build &gt; /tmp/phase-08-1-build.log 2&gt;&amp;1; build=$?; (cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api &amp;&amp; npm run build) &gt; /tmp/phase-08-1-api-build.log 2&gt;&amp;1; api_build=$?; (cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api &amp;&amp; NODE_ENV=development DISABLE_BACKGROUND_WORKERS=true node dist/scripts/verify-episode-audio-contract.js) &gt; /tmp/phase-08-1-api-verify.log 2&gt;&amp;1; api_verify=$?; if [ "$angular" -ne 0 ]; then if grep -Eiq "No binary for ChromeHeadless|Cannot find Chrome binary|ChromeHeadless.*(launcher|executable).*(not found|missing)" /tmp/phase-08-1-angular.log; then echo "ChromeHeadless unavailable; browser gate is limited"; else cat /tmp/phase-08-1-angular.log; exit 1; fi; fi; test "$build" -eq 0 -a "$api_build" -eq 0 -a "$api_verify" -eq 0'</automated></verify>
  <done>08.1-VALIDATION.md contains final status, FORM-01 through FORM-05 sign-off, exact browser-runner evidence, and independent build/API evidence; real Angular failures fail the gate, while only a clearly identified unavailable runner can produce a limited result.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| implementation -> documentation | Documentation must not claim fields or authority the API does not expose. |
| web repository -> sibling API repository | Cross-repository contract verification can fail if DTOs or response semantics drift. |
| working tree -> phase artifacts | Existing user changes must remain outside the phase diff. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08.1-07 | Tampering | API/UI contract docs | medium | mitigate | Verify docs against source DTOs, OpenAPI, focused tests, and both build commands before completion. |
| T-08.1-08 | Denial of service | verification/build gate | low | accept | Browser-runner availability is reported explicitly; API/build checks remain mandatory and bounded. |
| T-08.1-SC | Tampering | npm installs | low | accept | No dependency installation or package change is planned; use existing lock/runtime state. |
</threat_model>

<verification>
Confirm the phase directory contains all four plan files, ROADMAP lists them in wave order, FORM-01 through FORM-05 are each mapped, the Angular build command from AGENTS.md is run, and 08.1-VALIDATION.md records status, sign-off, browser-runner evidence, and sibling API verification. Review git diff for accidental edits outside intended docs/plans.
</verification>

<success_criteria>
Phase 8.1 is executable, traceable, documented, and verified across the frontend/API boundary with explicit handling of browser-test availability and preservation of unrelated work.
</success_criteria>

<output>
Create `.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-03-SUMMARY.md` when done.
</output>
