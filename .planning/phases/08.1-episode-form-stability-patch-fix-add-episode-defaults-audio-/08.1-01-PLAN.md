---
phase: 08.1-episode-form-stability-patch-fix-add-episode-defaults-audio
plan: 01
type: execute
wave: 1
depends_on: [08.1-00]
files_modified:
  - /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/services/episode-audio-metadata.service.ts
  - /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/routes/episodes.routes.ts
  - /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/schemas/episode.ts
  - /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/docs/openapi.ts
  - /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/scripts/verify-episode-audio-contract.ts
autonomous: true
requirements: [FORM-02, FORM-04]
must_haves:
  truths:
    - "Authenticated episode-audio upload returns backend-confirmed HH:MM:SS duration and nonnegative byte count after completion; trailer-audio responses remain unchanged."
    - "Audio metadata extraction or confirmation failure rejects the upload and does not report a successful staged audio with blank required metadata."
    - "Episode create and update reject music-credit payloads unless at least one credit has a trimmed name and a trimmed reference URL."
  artifacts:
    - "A server-side audio metadata service validates ffprobe output and file bytes without browser-derived values."
    - "The upload route and episode write schema/documentation expose the agreed metadata and music-credit contract."
    - "A repeatable API verification script covers success, metadata failure, trailer non-regression, and music-credit rejection."
  key_links:
    - "makeUploadRoute('audio') invokes metadata extraction before staging success is returned, while makeUploadRoute('trailer') does not."
    - "Draft audio upload response -> Angular editor state -> buildPayload -> POST /episodes -> persisted episode DTO -> GET/reload returns confirmed duration/bytes, with explicit upload/create cleanup on failure."
    - "The same complete-music-credit predicate is applied at both POST and PUT episode boundaries."
---

<objective>
Make the sibling API the authoritative source for episode-audio metadata and complete music-credit validation, preserving the existing trailer-audio contract.

Purpose: The Angular client must never infer required media metadata or become the business-rule authority.
Output: Tested API response/schema behavior and documented contract for Plan 02 to consume.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-CONTEXT.md
@.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-RESEARCH.md
@docs/ARCHITECTURE.md
@/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/routes/episodes.routes.ts
@/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/schemas/episode.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Complete the Wave 0 API contract coverage for audio metadata and music validation</name>
  <files>/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/scripts/verify-episode-audio-contract.ts</files>
  <behavior>
    - A valid audio metadata result is normalized to HH:MM:SS and retains the actual confirmed byte count.
    - Missing, non-finite, or unusable duration metadata causes an upload-contract failure; browser file size is never used as duration.
    - Trailer audio follows the existing filename/message response path without duration/bytes requirements.
    - A music credit with only a name or only a link is rejected, while one with both trimmed values is accepted.
  </behavior>
  <action>Extend the Wave 0 verifier created by 08.1-00 using the API repository's existing assertion/script conventions and package script. Exercise the pure metadata/validation seams and route response fixtures or an in-process route harness; do not add a new test dependency. Include draft and persisted audio response shapes, failure cleanup expectations, and explicit trailer non-regression assertions. Preserve the initial RED assertions, then make the complete verifier green only after the production helper is implemented. No package installation is required.</action>
  <verify><automated>cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api && npm run build && NODE_ENV=development DISABLE_BACKGROUND_WORKERS=true node dist/scripts/verify-episode-audio-contract.js</automated></verify>
  <done>The API repository has a repeatable command that fails for the current missing contract and passes only when metadata completeness, formatting, trailer isolation, and music-credit validation are all proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement authoritative audio metadata extraction and complete-credit write validation</name>
  <files>/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/services/episode-audio-metadata.service.ts, /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/routes/episodes.routes.ts, /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/schemas/episode.ts</files>
  <behavior>
    - Only the audio UploadSpec invokes the existing server-side ffprobe capability; trailer, cover, and trailer-video flows are untouched.
    - The route validates finite duration and confirmed staged-file bytes before returning success, deletes failed staging output, and returns an actionable 4xx/5xx error instead of blank required fields.
    - Existing episodes persist confirmed duration and bytes; draft uploads return the same fields for the subsequent create payload.
    - POST and PUT episode writes enforce at least one structured music credit containing a non-empty name and at least one non-empty reference URL, without moving authorization or persistence into Angular.
  </behavior>
  <action>Extract a small API service around execFile/ffprobe (or the already-established API probe seam), normalize duration to HH:MM:SS, use the staged file stat for bytes, validate all outputs, and keep the implementation dependency-free. Integrate it only in the audio branch of makeUploadRoute before the success response and before persisted media metadata is finalized. Return confirmed duration/bytes in the new-episode draft response, ensure Angular can carry them through POST /episodes, and verify the existing create path persists them and returns them after staged-media promotion; retain only the documented retryable staged source on create failure and remove the staged file on probe/upload failure. Add a shared schema/predicate helper that safely parses the existing JSON-string musicCredits representation, trims values, requires name plus URL, and is applied by episodeSchema at both create and update boundaries. Preserve legacy response fields, auth middleware, Multer constraints, transcription behavior, and trailer-audio behavior. Use the locked decisions D-04, D-05, D-08, and D-11 explicitly in implementation comments or task traceability.</action>
  <verify><automated>cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api && npm run build && NODE_ENV=development DISABLE_BACKGROUND_WORKERS=true node dist/scripts/verify-episode-audio-contract.js</automated></verify>
  <done>API contract verification passes; audio success includes confirmed duration/bytes, metadata failure is non-success with staged cleanup, trailer audio remains compatible, and incomplete music credits cannot be created or updated.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the API contract in OpenAPI</name>
  <files>/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/docs/openapi.ts</files>
  <action>Update the episode and audio-upload schemas/examples to state that audio completion returns backend-confirmed duration in HH:MM:SS and integer bytes, that these fields are required for a successful audio upload response, and that incomplete music credits are rejected at episode writes. Keep trailer-audio documented as the existing filename/transcription response and do not expose ffprobe internals.</action>
  <verify><automated>cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api && npm run build</automated></verify>
  <done>Generated API documentation matches the implementation and clearly distinguishes audio metadata from trailer-audio behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated client -> API upload route | Untrusted multipart MP3 crosses into filesystem and metadata extraction. |
| authenticated client -> episode create/update | Untrusted JSON credit and metadata fields cross into persistence. |
| API -> ffprobe process | Uploaded file path and probe output cross a local process boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08.1-01 | Tampering | audio upload metadata | high | mitigate | Keep auth/Multer checks, probe only server-staged files, validate finite duration and stat bytes, and reject incomplete output. |
| T-08.1-02 | Denial of service | ffprobe invocation | medium | mitigate | Use the existing upload size limit, bounded process/error handling, and cleanup on probe failure. |
| T-08.1-03 | Tampering | episode musicCredits | high | mitigate | Parse and validate trimmed name plus reference URL in the API schema before repository writes. |
| T-08.1-SC | Tampering | npm installs | low | accept | No new package is introduced; existing runtime ffprobe capability is reused and build verification is required. |
</threat_model>

<verification>
Run the API build and episode-audio contract verification command from a clean API process configuration, then inspect the generated OpenAPI typecheck. Confirm no frontend code is required to calculate duration or bytes and no trailer-audio response assertion changed.
</verification>

<success_criteria>
The sibling API exposes a stable, backend-confirmed audio metadata contract and rejects incomplete music credits while preserving all existing media routes.
</success_criteria>

<output>
Create `.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-01-SUMMARY.md` when done.
</output>
