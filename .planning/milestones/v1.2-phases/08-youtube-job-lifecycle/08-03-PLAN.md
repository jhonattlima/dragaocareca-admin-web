---
phase: 08-youtube-job-lifecycle
plan: 03
type: execute
wave: 2
depends_on: [08-00, 08-01]
files_modified:
  - src/app/core/api.service.ts
  - src/app/core/api.service.spec.ts
autonomous: true
requirements: [YOUTUBE-01, YOUTUBE-02, YOUTUBE-03, YOUTUBE-04, YOUTUBE-05, OPS-01, OPS-02, OPS-03, OPS-04]
must_haves:
  truths:
    - "Angular has typed authenticated wrappers for metadata-bearing start, current lookup, status, same-job retry, and cancellation using the exact API URLs and bodies."
    - "The browser DTO type exposes operator-safe state/progress/error/retry/cancellation/private-watch-link fields only."
  artifacts:
    - "ApiService contains YouTube lifecycle types and wrappers."
    - "HttpTestingController tests lock URLs, bodies, response typing, and sensitive-field absence."
  key_links:
    - "AuthInterceptor -> ApiService YouTube routes"
    - "API sanitized DTO -> ManageComponent presentation mapping"
---

<objective>
Add the thin Angular HTTP boundary for the durable YouTube lifecycle.

Purpose: Let the existing Manage workflow consume the sibling API without browser OAuth, provider calls, or business logic.
Output: Safe typed DTOs, start/current/status/retry/cancel wrappers, and request-contract tests.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-youtube-job-lifecycle/08-CONTEXT.md
@.planning/phases/08-youtube-job-lifecycle/08-PATTERNS.md
@src/app/core/api.service.ts
@src/app/core/api.service.spec.ts
@../dragaocareca-admin-api/src/docs/openapi.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define safe YouTube snapshot types and HTTP wrappers</name>
  <files>src/app/core/api.service.ts, src/app/core/api.service.spec.ts</files>
  <behavior>
    - Start sends POST /episodes/{episodeId}/youtube-trailer-jobs with the selected/current title and summary and accepts 202/200 snapshots.
    - Current lookup, status, same-job retry, and cancel use the exact authenticated API paths and empty-body POSTs where required.
    - Snapshot typing includes raw API statuses, transfer bytes, nullable processing fields, cancellation boundary, normalized error category, retry timing, timestamps, and nullable sanitized privateWatchUrl only; it has no provider/session/source/raw-error fields.
  </behavior>
  <read_first>src/app/core/api.service.ts, src/app/core/api.service.spec.ts, src/app/core/auth.interceptor.ts, .planning/phases/08-youtube-job-lifecycle/08-PATTERNS.md</read_first>
  <action>Follow the existing ApiService artifact-wrapper pattern and auth interceptor boundary. Add the exact public snapshot/status union matching the backend, including privateWatchUrl as a nullable sanitized URL rather than a provider identifier. Implement metadata-bearing start with the current operator title and summary, plus current, status, retry, and cancel methods. Add focused HttpTestingController fixtures for queued/claimed/transferring/processing/ready/failed/cancel_requested/cancelled/obsolete, 202 versus 200 handling, no-store-compatible responses, accepted metadata, and compile-time-visible safe fields. Per D-01/D-11, never accept provider/path/credential fields, never import YouTube SDK/OAuth, and do not add Angular publish methods; the existing API publish route remains an API-owned contract verified in the backend plan.</action>
  <verify><automated>npm test -- --watch=false --browsers=ChromeHeadless --include='src/app/core/api.service.spec.ts' && npm run build</automated></verify>
  <done>Angular can call every Phase 8 job endpoint with exact contracts, and tests prevent browser-facing provider secrets, sessions, paths, raw errors, or publish controls from entering the service boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Lock request and safe-field fixtures at the Angular boundary</name>
  <files>src/app/core/api.service.spec.ts</files>
  <read_first>src/app/core/api.service.spec.ts, src/app/core/api.service.ts, ../dragaocareca-admin-api/src/docs/openapi.ts, .planning/phases/08-youtube-job-lifecycle/08-VALIDATION.md</read_first>
  <action>Add focused HttpTestingController assertions for exact start/current/status/retry/cancel URLs, metadata request bodies, response status handling, and fixtures for every raw backend lifecycle status. Assert that the fixture shape accepts privateWatchUrl only as a sanitized nullable URL and has no provider ID, session URI, source identity, raw error, OAuth, path, or lease field. Add a contract test proving title/summary are accepted while provider/path/credential fields are rejected, preserving YOUTUBE-01 and the no-Angular-publish-controls boundary.</action>
  <verify><automated>npm test -- --watch=false --browsers=ChromeHeadless --include='src/app/core/api.service.spec.ts' && npm run build</automated></verify>
  <done>Angular request/response tests make the Phase 8 metadata and safe DTO boundary executable and prevent provider-secret or publication-control expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Angular -> API | Authenticated browser requests carry only episode/job IDs and empty control bodies. |
| API DTO -> TypeScript | Untrusted response data is constrained to the sanitized public contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09 | Elevation/Tampering | ApiService routes | high | mitigate | Preserve centralized bearer interceptor, exact endpoint IDs, and backend validation; never send provider credentials or source paths. |
| T-08-10 | Information disclosure | frontend DTO types/tests | high | mitigate | Allowlist snapshot fields and assert forbidden provider/session/source/raw-error fields are absent. |
| T-08-SC | Tampering | npm installs | high | mitigate | No package installation; use the package-locked Angular test command. |
</threat_model>

<verification>
Run the focused ApiService suite and `npm run build` after the dependent Manage plan is complete.
</verification>

<success_criteria>
The Angular HTTP boundary supports the Phase 8 YOUTUBE-01 through YOUTUBE-05 and OPS-01 through OPS-04 contract, is typed and authenticated through existing infrastructure, is contract-tested, and is incapable of initiating direct YouTube/provider operations or publication controls.
</success_criteria>

<output>
Create `.planning/phases/08-youtube-job-lifecycle/08-03-SUMMARY.md` when done.

## Artifacts this phase produces

- Typed Angular wrappers for metadata-bearing start, current lookup, status, same-job retry, and cancellation.
- Contract tests for accepted title/summary, sanitized privateWatchUrl, safe-field omission, and no Angular publish controls.
</output>
