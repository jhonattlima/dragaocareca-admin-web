---
phase: 08.1
slug: episode-form-stability-patch-fix-add-episode-defaults-audio
status: limited
nyquist_compliant: false
wave_0_complete: true
created: 2026-08-14
---

# Phase 08.1 — Validation Strategy

## Test Infrastructure

| Property | Value |
|---|---|
| Frontend | Angular CLI 15, Karma, Jasmine |
| Frontend browser command | `npm test -- --watch=false --browsers=ChromeHeadless` |
| Frontend build command | `npm run build` |
| Sibling API build | `cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api && npm run build` |
| Sibling API contract | `cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api && NODE_ENV=development DISABLE_BACKGROUND_WORKERS=true node dist/scripts/verify-episode-audio-contract.js` |

## Wave 0 Coverage

Wave 0 scaffolds are required before implementation and are owned by `08.1-00-PLAN.md`. They intentionally fail until the API and Angular changes complete. The initial artifact state is therefore `status: planned`, `wave_0_complete: false`, and `nyquist_compliant: false`; Plan 00 must execute before Plans 01–03.

| Requirement | Wave 0 automated coverage | Planned artifact | Owner |
|---|---|---|---|
| FORM-01 | Deterministic latest-date, seven-calendar-day, empty-list, invalid-row, and local-clock tests | `src/app/pages/manage/manage.component.spec.ts` | 08.1-00, completed by 08.1-02 |
| FORM-02 | Audio metadata success/missing-field failure, fixed two-decimal decimal-MB presentation, draft-to-create persistence/reload, cleanup, and trailer non-regression contract tests | `src/app/core/api.service.spec.ts`; sibling `verify-episode-audio-contract.ts` | 08.1-00, completed by 08.1-01/02 |
| FORM-03 | Configured participant names intersect `ManageComponent.memberOptions`; unknown names are ignored | `src/app/pages/manage/manage.component.spec.ts` | 08.1-00, completed by 08.1-02 |
| FORM-04 | Partial music credits fail; trimmed name plus trimmed reference URL passes UI/API boundary | `src/app/pages/manage/manage.component.spec.ts`; sibling contract verifier | 08.1-00, completed by 08.1-01/02 |
| FORM-05 | Duration, Bytes, and Spotify ID are native readonly/light-gray fields with no hint requirement | `src/app/pages/manage/manage.component.spec.ts` | 08.1-00, completed by 08.1-02 |

## Required Verification Sequence

Run each command independently. A missing ChromeHeadless binary is recorded as a browser-runner limitation; it must not prevent the build or either API command from running.

1. `npm test -- --watch=false --browsers=ChromeHeadless`
2. `npm run build`
3. `cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api && npm run build`
4. `cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api && NODE_ENV=development DISABLE_BACKGROUND_WORKERS=true node dist/scripts/verify-episode-audio-contract.js`

The contract verifier must cover the complete path: API draft audio response → Angular editor state → `buildPayload` → `POST /episodes` → persisted episode DTO → reload, including probe/upload failure cleanup and create failure cleanup/retry semantics. It must also prove trailer-audio behavior remains unchanged.

Angular gate interpretation is strict: a real Jasmine/Karma assertion failure or compilation error is a failed phase gate. Only an explicitly matched missing ChromeHeadless/Chrome binary or launcher is classified as runner-unavailable; in that case the browser result remains limited/pending, while frontend build and both API commands still run and must pass. Plan 03 records the exact output and classification here.

## Sign-Off

- [x] FORM-01 through FORM-05 implementation contracts are covered by the focused Angular/API artifacts and the independent API verifier; browser execution remains limited by the unavailable launcher.
- [x] Frontend build passes.
- [x] Sibling API build and contract verifier pass.
- [x] ChromeHeadless limitation is recorded without converting build success into test success.

## Execution Evidence

Final execution: 2026-08-14. All commands were run independently; the frontend working tree was clean before and after verification. The sibling API had pre-existing unrelated changes (`.planning/config.json`, Phase 18/research artifacts, and `skills/`), which were not staged or modified.

| Gate | Command | Exit status | Evidence / classification |
|---|---|---:|---|
| Angular browser suite | `npm test -- --watch=false --browsers=ChromeHeadless` | 1 | **Limited, runner unavailable.** Bundle generation completed, then Karma reported `No binary for ChromeHeadless browser on your platform. Please, set "CHROME_BIN" env variable.` No Jasmine/Karma assertion or compilation failure was reported. Browser tests are not marked passed. |
| Frontend build | `npm run build` | 0 | Passed. Existing Angular selector and bundle/style budget warnings remain non-fatal. |
| Sibling API build | `cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api && npm run build` | 0 | Passed (`tsc -p tsconfig.json`). |
| API contract verifier | `cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api && NODE_ENV=development DISABLE_BACKGROUND_WORKERS=true node dist/scripts/verify-episode-audio-contract.js` | 0 | Passed: `Episode audio contract verification passed.` Covers draft/upload response through Angular payload, create/persisted DTO/reload, probe/upload/create cleanup, complete-credit validation, and trailer-audio compatibility. |

FORM-01 through FORM-05 sign-off is complete from the shipped frontend/API implementation, focused contract artifacts, successful frontend/API builds, and the passing API verifier. The only unavailable evidence is browser execution itself; no build success is being substituted for that test result.

**Final status: `limited`** — the only nonzero gate is the explicitly missing ChromeHeadless binary; all build and API gates passed, and no real test failure occurred.
