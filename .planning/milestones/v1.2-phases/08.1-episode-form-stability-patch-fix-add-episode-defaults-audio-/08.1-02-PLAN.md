---
phase: 08.1-episode-form-stability-patch-fix-add-episode-defaults-audio
plan: 02
type: execute
wave: 2
depends_on: [08.1-01]
files_modified:
  - src/app/core/api.service.ts
  - src/app/core/api.service.spec.ts
  - src/app/pages/manage/manage.component.ts
  - src/app/pages/manage/manage.component.spec.ts
  - src/app/pages/manage/episode-form.component.html
  - src/app/pages/manage/episode-form.component.scss
  - src/environments/environment.ts
  - src/environments/environment.prod.ts
  - src/environments/environment.staging.ts
autonomous: true
requirements: [FORM-01, FORM-03, FORM-05]
must_haves:
  truths:
    - "A new editor uses the latest loaded episode's local publication date/time plus seven calendar days, or current local date/time when the list is empty, and does not overwrite an active edit."
    - "After a completed episode-audio response, the form displays only API-confirmed HH:MM:SS duration and decimal MB bytes; a response missing either required field is treated as upload failure. Trailer-audio mapping remains filename-only."
    - "Configured participant names are editable in frontend environment files and only matching current member catalog options are selected for a new editor."
    - "Save stays disabled or visibly invalid until one music credit has both a trimmed name and a trimmed reference link."
    - "Duration, Bytes, and Spotify ID are native readonly fields with light-gray styling and no helper hints."
  artifacts:
    - "Typed Angular API model and upload mapper for backend-confirmed audio duration/bytes."
    - "ManageComponent defaults/configuration/validation behavior covered by focused Jasmine specs."
    - "Episode form template and stylesheet visibly implement all three read-only fields and music validation state."
  key_links:
    - "HttpResponse handling gates metadata assignment on kind === 'audio' and refuses incomplete API responses before setting success."
    - "The completed audio response is retained in editor state, buildPayload includes confirmed duration/bytes, createEpisode posts that payload, and the returned persisted DTO plus reload populate the same fields; upload/create failure resets UI state and preserves or cleans files according to the API contract."
    - "environment participant defaults intersect memberOptions during new-editor initialization after episode/catalog state is available."
    - "isEpisodeSaveDisabled and buildPayload/serialization use the same complete music-credit predicate."
---

<objective>
Make the Angular Add Episode workflow consume the API contract, apply reliable defaults, and present the locked validation/read-only behavior.

Purpose: Keep the browser thin while making operator-visible form state truthful and stable.
Output: Tested ManageComponent/API/template changes that integrate with Plan 01's API contract.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-CONTEXT.md
@.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-RESEARCH.md
@docs/README.md
@docs/ARCHITECTURE.md
@docs/CONFIGURATION.md
@src/app/pages/manage/manage.component.ts
@src/app/core/api.service.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Complete the Wave 0 Angular contract tests</name>
  <files>src/app/pages/manage/manage.component.spec.ts, src/app/core/api.service.spec.ts</files>
  <behavior>
    - Latest valid pubDate wins, seven calendar days are added without losing the local clock, empty episodes use current date/time, and invalid rows do not poison the suggestion.
    - Audio HttpResponse with duration and bytes updates the editor only after completion and formats bytes as decimal MB; missing metadata reports failure and does not report staged success.
    - Trailer HttpResponse remains compatible and does not receive audio metadata requirements.
    - Configured defaults select matching member names only, ignore unknown names, and do not overwrite an active edit.
    - Music Save eligibility requires trimmed name plus at least one trimmed non-empty URL; partial entries remain blocked.
    - Template assertions find readonly attributes/classes on Duration, Bytes, and Spotify ID and no hint text is introduced for those fields.
  </behavior>
  <action>Complete the Angular specs scaffolded by 08.1-00 using the existing Jasmine/Karma seams instead of replacing unrelated working-tree tests. Use deterministic clock/date fixtures and mocked HttpEvents/ApiService responses. Add a test construction seam only if needed to exercise private helpers; do not test browser File.size as authoritative metadata. Assert the resolved fixed two-decimal decimal-MB rule with deterministic nonnegative half-up rounding while retaining raw confirmed bytes. Preserve existing trailer-video and transcription tests. Cite D-01 through D-13 in the test descriptions or neighboring test comments for traceability.</action>
  <verify><automated>npm test -- --watch=false --browsers=ChromeHeadless</automated></verify>
  <done>Focused tests exist for FORM-01 through FORM-05 and fail against each currently missing behavior without weakening existing phase-8 lifecycle assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement defaults, API mapping, participant configuration, and Save validation</name>
  <files>src/app/core/api.service.ts, src/app/pages/manage/manage.component.ts, src/environments/environment.ts, src/environments/environment.prod.ts, src/environments/environment.staging.ts</files>
  <behavior>
    - The API Episode type accepts confirmed duration/bytes without making the frontend extract or infer them.
    - computeSuggestedNextPubDate and initialization use calendar-day addition and local datetime conversion per D-01/D-02/D-03, including reset/new-editor races.
    - Audio-only response handling requires backend duration and bytes, formats decimal MB to exactly two decimal places using deterministic nonnegative half-up rounding while retaining the confirmed integer for payloads, and leaves trailer handling unchanged per D-04/D-05/D-06/D-07/D-08.
    - Participant defaults come from one clearly named environment array and are filtered against memberOptions per D-09/D-10.
    - A shared complete-music-credit predicate gates Save and aligns serialized payloads with D-11 and the API validation.
  </behavior>
  <action>Implement the smallest component/API orchestration changes around the existing loadEpisodes, scheduleAddEditorDefaults, uploadMedia HttpResponse branch, isEpisodeSaveDisabled, and buildPayload seams. Handle absent/invalid API metadata as an upload error with progress reset, no success message, and no false staged state; never use File.size for required bytes. Retain confirmed duration/bytes in the editor through createEpisode, assert the POST response contains the persisted DTO, and ensure reload rehydrates the same fields. Display bytes as decimal MB by dividing confirmed integer bytes by 1,000,000, rounding nonnegative values half-up to the nearest hundredth with integer arithmetic, and formatting exactly two decimal places; keep raw confirmed bytes available to payload code. Add the participant-default environment key to all configured environment variants while preserving authBypass exactly. Apply defaults only to the new editor after the authoritative memberOptions catalog is available and never during an active edit. Keep all business validation mirrored in the API rather than trusting the disabled button. Do not alter trailer-audio behavior. Implement after Task 1 tests are red, then make them green and refactor shared predicates/helpers without changing unrelated state flows.</action>
  <verify><automated>npm test -- --watch=false --browsers=ChromeHeadless</automated></verify>
  <done>All focused behavior tests pass, API-confirmed metadata is the only source of displayed audio fields, configured participant defaults are safely intersected, and Save/read-only behavior matches every locked decision.</done>
</task>

<task type="auto">
  <name>Task 3: Render validation feedback and read-only styling in the sectioned form</name>
  <files>src/app/pages/manage/episode-form.component.html, src/app/pages/manage/episode-form.component.scss</files>
  <action>Keep the existing legacy-inspired sections and form-first layout. Add concise music-credit validation feedback tied to the same controller predicate used by Save, while retaining the disabled Save behavior. Add a shared light-gray read-only class to Duration, Bytes, and Spotify ID, retain native readonly semantics, and omit helper/hint copy for those fields per D-12/D-13. Ensure labels, bindings, upload cards, participant cards, and unrelated controls remain unchanged.</action>
  <verify><automated>npm test -- --watch=false --browsers=ChromeHeadless</automated></verify>
  <done>Template tests and source inspection confirm the three fields cannot be edited, have the requested light-gray presentation only, and music-credit feedback is actionable without changing the sectioned layout.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| API response -> Angular state | Backend JSON is untrusted until required audio fields are validated. |
| environment configuration -> participant selection | Configured strings must not inject names outside the current catalog. |
| operator form -> API write | Browser validation is advisory and must remain paired with API enforcement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08.1-04 | Tampering | upload response mapper | high | mitigate | Accept audio metadata only in completed audio responses and require both fields before success. |
| T-08.1-05 | Spoofing | participant defaults | low | mitigate | Intersect environment names with the known member catalog before changing authors. |
| T-08.1-06 | Tampering | Save gate | medium | mitigate | Use the same trimmed complete-credit predicate for UI feedback/payload preparation while API remains authoritative. |
| T-08.1-SC | Tampering | npm installs | low | accept | No new frontend dependency is planned; existing Angular/RxJS stack is reused. |
</threat_model>

<verification>
Run the focused Angular suite with ChromeHeadless when available; if the browser is unavailable, record that limitation separately and still run TypeScript/build verification in Plan 03. Confirm authBypass remains unchanged in every environment file.
</verification>

<success_criteria>
Operators see truthful audio metadata, stable publication defaults, safe participant defaults, complete-credit Save gating, and clearly read-only metadata fields without any trailer-audio regression.
</success_criteria>

<output>
Create `.planning/phases/08.1-episode-form-stability-patch-fix-add-episode-defaults-audio-/08.1-02-SUMMARY.md` when done.
</output>
