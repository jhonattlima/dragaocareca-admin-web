---
phase: 09-title-hashtag-authoring
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - src/app/pages/manage/episode-form.component.html
  - src/app/pages/manage/episode-form.component.scss
autonomous: true
requirements: [TITLE-01, TITLE-02, TITLE-03, TITLE-04]
must_haves:
  truths:
    - "The sectioned episode form shows an editable hashtag field beside the read-only Spotify ID field."
    - "The trailer title preview appears below the form fields as a read-only control/preview and visibly reports the 100-Unicode validation error without offering title editing."
    - "Each authored hashtag exposes approximate count/retrieval feedback on hover and keyboard focus, with accessible dismissal behavior and recoverable unavailable messaging."
    - "Existing Save, reset, upload, and YouTube lifecycle controls remain in the established legacy-inspired layout and use the guards/state from ManageComponent."
  artifacts:
    - "Template bindings for authored hashtag input, suggestion busy/disabled state, read-only trailer title, validation, token feedback, and dismissal events."
    - "Scoped stylesheet rules for hashtag field/token/popover/validation states consistent with existing Bootstrap section blocks."
  key_links:
    - "Template events call ManageComponent orchestration methods rather than embedding business or provider logic."
    - "The title preview renders the same computed value and validity used by Save/start guards."
    - "Popup hover/focus and outside/blur/tab dismissal operate on the same transient lookup state used by the one-second debounce."
---

<objective>
Expose Phase 9 authoring behavior in the existing sectioned Manage form without changing its functional layout: editable hashtags beside Spotify ID, read-only computed title below, and accessible approximate lookup feedback.

Purpose: D-05/D-06 define the placement and capacity, while D-01/D-02/D-10/D-11/D-12 define the read-only preview and discoverable transient feedback. This plan is presentation-only; API and business decisions remain in Plans 09-01 and 09-02.
Output: Template and scoped styling wired to the completed ManageComponent state, verified by focused tests and production build.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-title-hashtag-authoring/09-CONTEXT.md
@.planning/phases/09-title-hashtag-authoring/09-RESEARCH.md
@docs/README.md
@docs/ARCHITECTURE.md
@src/app/pages/manage/episode-form.component.html
@src/app/pages/manage/episode-form.component.scss
@src/app/pages/manage/manage.component.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add sectioned hashtag and read-only title controls</name>
  <files>src/app/pages/manage/episode-form.component.html</files>
  <action>Place the separate editable hashtag input beside the existing read-only Spotify ID field per D-05, bind it to authored hashtag state, cap/display the three-token policy from D-06, disable it only during the API automatic hashtag-research/upload state per D-07, and wire input/focus/blur/hover/keyboard/outside-click handlers to ManageComponent orchestration methods. Add the read-only trailer-name field below the existing form fields using the computed value from Plan 09-02, with explicit validation text below it and no editable title binding, per D-01/D-02/D-03/D-04. Render normalized token feedback with approximate count and retrieval time, and distinguish recoverable unavailable states without blocking manual authoring. Preserve all existing sections, controls, auth behavior, and Save/start lifecycle bindings.</action>
  <verify><automated>npm test -- --watch=false --browsers=ChromeHeadless --include='src/app/pages/manage/manage.component.spec.ts'</automated></verify>
  <done>Template visibly provides the required editable hashtag authoring surface and read-only title/validation/lookup feedback while remaining wired to component orchestration only.</done>
</task>

<task type="auto">
  <name>Task 2: Style accessible hashtag tokens, popup, and validation states</name>
  <files>src/app/pages/manage/episode-form.component.scss</files>
  <action>Extend the existing Manage form styles with scoped rules for the hashtag field, disabled automatic state, focusable token affordances, transient approximate-count popup, unavailable/retry messaging, read-only trailer title, and over-limit validation. Keep contrast, keyboard focus visibility, responsive Bootstrap column behavior, and the established sectioned legacy-inspired visual language; do not introduce a new UI framework, collapse sections, or encode business rules in CSS.</action>
  <verify><automated>npm run build</automated></verify>
  <done>Build succeeds and the new controls have coherent responsive/focus/error styling without altering unrelated layout or application source boundaries.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM/operator interaction → template bindings | Untrusted text and pointer/keyboard events enter Angular handlers. |
| template → ManageComponent/API state | Presentation events invoke the already validated orchestration layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07 | Spoofing | lookup popup/token UI | low | mitigate | Label approximate/retrieved data clearly and render only the typed safe DTO from the API. |
| T-09-08 | Tampering | form event bindings | medium | mitigate | Keep all mutations and validation in ManageComponent; template only binds to named handlers and computed state. |
| T-09-09 | Information disclosure | popup/error rendering | low | mitigate | Avoid rendering raw provider errors or sensitive API fields; show normalized user-facing category/retry text only. |
| T-09-SC | Tampering | npm installs | high | accept | No package installation is planned; `npm run build` is the dependency integrity gate. |
</threat_model>

<verification>
Run the focused ManageComponent spec, `npm run build`, and the full Phase 9 Angular/API verification commands from the prior plans. Manually inspect the Manage form at desktop and narrow widths for field placement, read-only title behavior, keyboard focus, hover/focus popup dismissal, and preserved sectioned layout.
</verification>

<success_criteria>
Operators can edit hashtags beside Spotify ID, see a non-editable computed Trailer title with clear 100-Unicode validation, inspect approximate lookup feedback through hover/focus, and continue using the existing Save/start/commit workflow without layout regression.
</success_criteria>

<output>
Create `.planning/phases/09-title-hashtag-authoring/09-03-SUMMARY.md` when done.
</output>
