---
phase: 11-trailer-artifact-compatibility-release
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/app/core/api.service.spec.ts
  - src/app/pages/manage/manage.component.spec.ts
  - .planning/phases/11-trailer-artifact-compatibility-release/11-VERIFICATION.md
autonomous: false
requirements: [ARTIFACT-01, ARTIFACT-02]
must_haves:
  truths:
    - "Focused API and Manage tests prove the exact trailer-video selector is sent in the existing artifact job request."
    - "A finalized trailer DTO exposes and enables Trailer video .mp4, while absent/staged DTO state leaves it unavailable and unsent."
    - "A backend unavailable/no-final-file response does not fabricate a download and preserves the existing recoverable unavailable/partial behavior."
    - "Completed trailer-containing jobs use the existing generic authenticated native download path, and existing progress, stale job guard, retry, reset, and delivery cleanup tests remain green."
    - "The sibling API artifact verifier still proves canonical finalized trailer.mp4 ZIP behavior and stale/unavailable exclusion; frontend release gates remain green."
  artifacts:
    - path: "src/app/core/api.service.spec.ts"
      provides: "HTTP request-body regression coverage for the canonical trailer-video selector."
    - path: "src/app/pages/manage/manage.component.spec.ts"
      provides: "Modal availability, exact payload, unavailable/preflight rejection, completed delivery, and existing recovery regression coverage."
    - path: ".planning/phases/11-trailer-artifact-compatibility-release/11-VERIFICATION.md"
      provides: "Recorded automated gate results, known warnings, and any ChromeHeadless/live verification limitation."
  key_links:
    - from: "ManageComponent artifact option selection"
      to: "ApiService.startEpisodeArtifactJob"
      via: "checked selector array contains trailer-video unchanged"
    - from: "API start/preflight response"
      to: "ManageComponent unavailable state"
      via: "existing error/missing handling, with no client-fabricated URL"
    - from: "completed artifact snapshot"
      to: "downloadEpisodeArtifact"
      via: "existing authenticated HttpResponse<Blob> delivery and cleanup"
---

<objective>
Prove the trailer artifact integration and compatibility release gates with focused regressions and the already-existing backend verifier. Add no new artifact lifecycle or delivery implementation.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/README.md
@docs/ARCHITECTURE.md
@docs/CONFIGURATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.planning/phases/11-trailer-artifact-compatibility-release/11-RESEARCH.md
@.planning/milestones/v1.1-phases/04-artifact-job-contract/04-VERIFICATION.md
@.planning/milestones/v1.1-phases/05-episode-download-modal/05-VERIFICATION.md
@.planning/milestones/v1.1-phases/06-browser-download-release-validation/06-VERIFICATION.md
@.planning/phases/11-trailer-artifact-compatibility-release/11-01-PLAN.md
@src/app/core/api.service.spec.ts
@src/app/pages/manage/manage.component.spec.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add focused API and modal trailer-video regressions</name>
  <files>src/app/core/api.service.spec.ts, src/app/pages/manage/manage.component.spec.ts</files>
  <action>Read and preserve existing dirty source/spec changes before editing. Update artifact test fixtures and expectations for the new catalog row without weakening existing five-selector behavior. Add API coverage that starts a job with `trailer-video` in the request body and asserts the exact POST route/body; do not add a new wrapper. Add Manage coverage for: a finalized `trailerVideoFileName` exposing/enabling/checking the Trailer video .mp4 row; absent/null/staged-like DTO state disabling it and excluding it from the submitted selector list; exact selector payload when selected; a completed snapshot whose available/requested arrays contain `trailer-video` reaching the existing generic download path; and a 404/no-final-file or unavailable start response marking the option unavailable without inventing a URL or bypassing API preflight. Keep assertions for option order, empty selection, duplicate starts, progress labels, partial missing labels, authenticated native delivery, unsafe server filename rejection, same-job delivery retry, reset/new-job behavior, object URL cleanup, polling teardown, and stale job-ID protection. Use the finalized DTO filename only as the modal hint and leave stale replacement correctness to the backend verifier.</action>
  <verify><automated>./node_modules/.bin/tsc -p tsconfig.spec.json --noEmit && npm test -- --watch=false --browsers=ChromeHeadless --include='src/app/core/api.service.spec.ts' --include='src/app/pages/manage/manage.component.spec.ts' --include='src/app/pages/manage/phase6-progress-threshold.spec.ts' --include='src/app/pages/manage/phase6-validation.spec.ts'</automated></verify>
  <done>Focused API and Manage tests prove finalized/absent trailer-video availability, exact selector payload, backend rejection handling, generic completed delivery, and preservation of all prior artifact recovery contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Run backend artifact, frontend compatibility, and build gates</name>
  <files>.planning/phases/11-trailer-artifact-compatibility-release/11-VERIFICATION.md</files>
  <action>Run the sibling API's existing `src/scripts/verify-episode-artifact-downloads.ts` through its documented npm verification command and record the evidence relevant to finalized `trailer-video`, canonical `trailer.mp4`, unavailable/staged/failed/stale replacement exclusion, and no-job-on-empty-preflight. Do not modify the sibling API or compensate in Angular for a verifier failure. Then run spec compilation, the focused suite, the complete ChromeHeadless suite when a browser is available, and `npm run build`; record exit status and preserve known non-blocking selector-parser and Angular budget warnings. Confirm no package/lockfile changes and that the existing authenticated native download/retry/reset flow remains intact. If ChromeHeadless or live API evidence is unavailable, record it as a limitation rather than claiming a pass; the automated verifier, tests, and build still must be reported separately.</action>
  <verify><automated>cd ../dragaocareca-admin-api && npm run verify:episode-artifact-downloads && cd ../dragaocareca-admin-web && ./node_modules/.bin/tsc -p tsconfig.spec.json --noEmit && npm test -- --watch=false --browsers=ChromeHeadless && npm run build</automated></verify>
  <done>The API's authoritative trailer artifact verifier and frontend TypeScript/focused/full tests/build produce recorded release evidence; no new frontend compatibility failure or unrecorded limitation remains.</done>
</task>
</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Finalized trailer video flows through existing artifact job/ZIP without staged or stale exposure | 11-01, 11-02 | COVERED | UI contract, API verifier, regression suite, and build gates cover the complete goal.
REQ | ARTIFACT-01 | Modal offers finalized trailer-video | 11-01, 11-02 | COVERED | Catalog implementation and finalized/absent modal tests.
REQ | ARTIFACT-02 | Canonical trailer.mp4 inclusion and exclusion of unavailable/staged files | 11-01, 11-02 | COVERED | Exact selector test plus sibling API verifier for preflight, snapshot/evidence, and ZIP naming.
RESEARCH | R-01 | Existing job/poll/native delivery/retry/reset remain unchanged | 11-02 | COVERED | Regression and complete-suite gates exercise compatibility.
RESEARCH | R-02 | DTO filename is only a display hint; API remains authoritative | 11-01, 11-02 | COVERED | Unavailable/preflight-rejection tests do not fabricate download state.
RESEARCH | R-03 | Backend already supports selector and finalized-file preflight | 11-02 | COVERED | Existing verifier is rerun; no backend redesign is planned.
RESEARCH | R-04 | Dirty Phase 9/10 source is preserved | 11-01, 11-02 | COVERED | Explicit read-diff/preserve instructions and narrow file edits.
CONTEXT | user constraints | Minimal contract integration, no client ZIP or unrelated source changes, existing gates required | 11-01, 11-02 | COVERED | Tasks and verification enforce the scope fence.
</source_coverage_audit>

<verification>
Required release evidence: sibling API artifact verifier, spec compilation, focused artifact suites, complete frontend Karma/ChromeHeadless where available, and `npm run build`. Existing known warnings may be recorded but must not hide a nonzero exit or a new test failure.
</verification>

<success_criteria>
ARTIFACT-01 and ARTIFACT-02 have focused frontend and authoritative API evidence, while progress, authenticated native download, retry/reset, existing tests, and build compatibility remain green or have an explicitly recorded environment limitation.
</success_criteria>

<output>Create `.planning/phases/11-trailer-artifact-compatibility-release/11-02-SUMMARY.md` and `.planning/phases/11-trailer-artifact-compatibility-release/11-VERIFICATION.md` when executed.</output>
