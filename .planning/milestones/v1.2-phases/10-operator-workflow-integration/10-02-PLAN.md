---
phase: 10-operator-workflow-integration
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/app/pages/manage/manage.component.ts
  - src/app/pages/manage/manage.component.html
  - src/app/pages/manage/episode-form.component.html
  - src/app/pages/manage/episode-form.component.scss
  - src/app/pages/manage/manage.component.spec.ts
autonomous: true
requirements: [OPS-05]
must_haves:
  truths:
    - "Save persists the episode first, then reports YouTube commit/publication success or failure while the active editor retains title, hashtags, private link, and recovery state."
    - "A failed or stale asynchronous commit cannot write into a reset, replacement, or newer editor; an operator can understand the next action from the sectioned form."
    - "Local MP4 staging and the separate private YouTube lifecycle remain distinct, with honest in-flight, cancellation, replacement, private-ready, and actionable error states."
    - "Save without a YouTube job keeps the ordinary save/reset behavior compatible."
  artifacts:
    - path: "src/app/pages/manage/manage.component.ts"
      provides: "Explicit save/commit transaction state, identity snapshot, terminal handling, and stale callback guards."
      contains: "saveEpisode"
    - path: "src/app/pages/manage/episode-form.component.html"
      provides: "Operator-visible save/commit state and recovery actions in the existing sectioned form."
    - path: "src/app/pages/manage/manage.component.spec.ts"
      provides: "Deferred create/update and commit success/error/stale regression tests."
  key_links:
    - from: "saveEpisode(editor)"
      to: "ApiService createEpisode/updateEpisode"
      via: "persisted episode response and captured editor/job identity"
    - from: "episode save response"
      to: "ApiService commitYoutubeTrailerJob"
      via: "existing Save-time /commit contract with title and hashtags"
    - from: "commit terminal response"
      to: "active editor/template"
      via: "episode ID, job ID, source generation, and editor identity guards"

---

<objective>
Make Save-time YouTube commit feedback truthful without losing the operator’s editor context. Keep browser orchestration thin, use only the existing authenticated `/commit` contract, and preserve the legacy-inspired local/YouTube section split.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/README.md
@docs/ARCHITECTURE.md
@docs/CONFIGURATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.planning/phases/10-operator-workflow-integration/10-RESEARCH.md
@.planning/phases/08-youtube-job-lifecycle/08-04-PLAN.md
@.planning/phases/09-title-hashtag-authoring/09-VERIFICATION.md
@src/app/core/api.service.ts
@src/app/pages/manage/manage.component.ts
@src/app/pages/manage/episode-form.component.html
@src/app/pages/manage/manage.component.html
@src/app/pages/manage/manage.component.spec.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Sequence Save and YouTube commit as an identity-guarded transaction</name>
  <files>src/app/pages/manage/manage.component.ts, src/app/pages/manage/manage.component.spec.ts</files>
  <read_first>src/app/pages/manage/manage.component.ts, src/app/core/api.service.ts, src/app/pages/manage/manage.component.spec.ts, .planning/phases/10-operator-workflow-integration/10-RESEARCH.md</read_first>
  <action>Refactor saveEpisode() so episode create/update completion establishes the saved episode ID, then an active current YouTube job is committed through the existing ApiService commitYoutubeTrailerJob() call before the editor is reset/reloaded. Capture the editor object, episode ID, job ID, sourceGeneration, computed title, and serialized hashtags at transaction start. Expose explicit save/commit in-flight and terminal success/error state; keep the editor and job/private URL context on commit failure so retry/recovery is possible, and only perform the existing reset/reload at the chosen successful terminal boundary. If no job exists, retain ordinary save success/reset behavior. Guard every deferred callback against editor identity, episode ID, job ID, and source generation; clear or ignore callbacks after explicit reset, replacement, or destroy. Preserve backend-owned publication semantics: call `/commit`, never direct publish, OAuth, provider, or raw-error routes. Add a focused Manage regression assertion that hashtag lookup remains available in New Episode and that its success and error state survive both the episode save and the YouTube commit transaction boundaries. Do not change artifact selectors or Phase 11 download behavior.</action>
  <verify><automated>npm test -- --watch=false --browsers=ChromeHeadless --include='src/app/pages/manage/manage.component.spec.ts'</automated></verify>
  <done>Save does not claim publication before commit completion; commit success is visible before cleanup, commit failure leaves actionable editor/job context, ordinary saves remain compatible, and stale callbacks cannot mutate a newer editor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Present transaction and recovery states in the existing sectioned UI</name>
  <files>src/app/pages/manage/episode-form.component.html, src/app/pages/manage/episode-form.component.scss, src/app/pages/manage/manage.component.html, src/app/pages/manage/manage.component.ts, src/app/pages/manage/manage.component.spec.ts</files>
  <read_first>src/app/pages/manage/episode-form.component.html, src/app/pages/manage/episode-form.component.scss, src/app/pages/manage/manage.component.html, src/app/pages/manage/manage.component.ts, src/app/pages/manage/manage.component.spec.ts</read_first>
  <action>Add only the bindings and scoped presentation needed to distinguish local trailer upload/staging from the separate YouTube private-transfer/commit stage. Show Save/commit in-flight and terminal copy, retain the computed title, hashtags, summary, private link, cancellation boundary, replacement warning, retry/cancel affordances, and actionable normalized error text while a transaction is recoverable. Disable conflicting Save/replacement actions during the relevant in-flight operation without hiding the editor. Keep the existing form-first section blocks and accessible aria-live/status patterns; never label private readiness as public publication and never render provider credentials, IDs, sessions, raw errors, or browser-side provider actions.</action>
  <verify><automated>npm test -- --watch=false --browsers=ChromeHeadless --include='src/app/pages/manage/manage.component.spec.ts' && npm run build</automated></verify>
  <done>The template visibly separates local and YouTube stages, communicates commit/recovery truthfully, and preserves existing upload, edit, summary, cancellation, replacement, and artifact-modal behavior.</done>
</task>
</tasks>

<verification>
Automated coverage must include deferred create/update and commit success/error, reset/replacement stale callbacks, ordinary save without a job, and template-visible in-flight/error/recovery states. Run the focused suite and `npm run build`; retain the existing selector-parser and bundle/style budget warnings as non-blocking unless they become new failures.
</verification>

<success_criteria>
The complete New Episode path reaches local staging, private YouTube status/link review, authored title/hashtags, and honest Save-time publication feedback through existing API contracts without losing editor context or allowing stale state to overwrite current state; hashtag lookup remains available and its success/error state survives save and commit.
</success_criteria>

<output>Create `.planning/phases/10-operator-workflow-integration/10-02-SUMMARY.md` when done.</output>
