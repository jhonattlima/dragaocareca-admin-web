---
phase: 04-artifact-job-contract
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/scripts/verify-episode-artifact-downloads.ts
  - /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/services/episode-artifact-preparation.service.ts
  - /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/database/repositories/artifact-job.repository.ts
autonomous: false
requirements: [VAL-04]
must_haves:
  truths:
    - "Automated verification proves the full start -> poll -> complete -> authenticated-download lifecycle."
    - "Automated verification proves failure, cleanup, partial/missing reporting, duplicate protection, restart recovery, expiry, and invalid-selector/path rejection."
    - "Automated verification proves unauthenticated requests and mismatched job ownership cannot access status or ZIP bytes."
  artifacts:
    - path: "/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/scripts/verify-episode-artifact-downloads.ts"
      provides: "Compiled direct-router and service verifier for VAL-04"
  key_links:
    - from: "verify-episode-artifact-downloads.ts"
      to: "episodes.routes.ts"
      via: "direct invocation of all three route stacks with MemoryResponse"
    - from: "verify-episode-artifact-downloads.ts"
      to: "episode-artifact-preparation.service.ts"
      via: "deterministic worker processing, failure hook, restart, expiry, and cleanup assertions"
    - from: "verify-episode-artifact-downloads.ts"
      to: "src/docs/openapi.ts"
      via: "schema/path/security parity assertions"
---

<objective>
Complete executable API verification for every Phase 4 lifecycle, validation, security, cleanup, and documentation behavior.

Purpose: VAL-04 is the release gate for the backend contract and must catch drift before Phases 5–6 build against it.
Output: A deterministic compiled verifier covering the exact endpoint paths and all required success/error branches.
</objective>

<execution_prerequisite>
The sibling API workspace at `/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api` must be writable for verifier/service/repository source edits, `dist` output, isolated database/media/job fixtures, and builds. Before execution, run `test -w /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api && test -w /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/dist`. If either check fails, execute in an equivalent writable API checkout or CI workspace; the exact API and web commands below remain required.
</execution_prerequisite>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-artifact-job-contract/04-CONTEXT.md
@.planning/phases/04-artifact-job-contract/04-VALIDATION.md
@.planning/phases/04-artifact-job-contract/04-PATTERNS.md
@/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/scripts/verify-episode-artifact-downloads.ts
@.planning/phases/04-artifact-job-contract/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the verifier across lifecycle, partial, failure, and security cases</name>
  <files>/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/scripts/verify-episode-artifact-downloads.ts</files>
  <action>Rewrite the existing preparation assertions in `/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/scripts/verify-episode-artifact-downloads.ts` to call POST `/:episodeId/artifacts/jobs` with JSON bodies and poll GET `/jobs/:jobId`, then call `/download` only after the worker reaches completed. Follow the existing verifier’s fixture, MemoryResponse, direct-router, ZIP inspection, and auth-bypass patterns; preserve selector/path behavior from `/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/services/episode-artifact-download.service.ts`, lifecycle expectations from `/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/services/episode-transcription.service.ts` and `/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/workers/launch-notification.worker.ts`, persistence/reset checks from `/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/database/sqlite.ts` and `/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api/src/database/repositories/episode.repository.ts`, and the existing verifier itself. Isolate database/media/job roots between scenarios. Assert canonical selector normalization and rejection of arrays containing paths/filenames, unknown/empty selectors, invalid IDs, unknown episodes, and no-artifact requests with no persisted job. Assert pending -> processing -> completed, progress integer bounds and monotonic sequence, completed=100, requested/available/missing snapshots, deterministic safe ZIP filename, expected catalog-derived entries, authenticated binary download, no-store, and missing-artifact header. Exercise concurrent identical starts and prove one active job, mismatch/unknown job 404, unauthenticated start/status/download 401, failed archive with nonempty public error and removed partial/final outputs using the Wave 0 failure hook, startup recovery of interrupted processing, exactly-45-minute TTL expiry returning 404, and the explicit Phase 6/manual DC 334 prerequisite record. Include assertions that response bodies/log captures do not contain storage roots, path metadata, cache keys, or raw filesystem details. Ensure every fixture and hook is reset in finally blocks.</action>
  <deterministic_barrier>Use the injectable stage controller exported by Plan 04-01 Task 2. The verifier must release named stages synchronously, capture a status response after each release, and assert pending at 0, each processing stage has nondecreasing progress, and completed is 100. Do not use timer polling or scheduling luck to discover lifecycle transitions; separately release the failure stage and assert persisted error plus cleanup.</deterministic_barrier>
  <verify>
    <automated>cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api && npm run build && npm run verify:episode-artifact-downloads</automated>
  </verify>
  <done>The verifier passes for full, partial, failure, empty/missing, duplicate, restart, expiry, auth, ownership, invalid-selector, invalid-path, cleanup, and progress-monotonicity scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: Add OpenAPI and full-suite regression gates to the verifier</name>
  <files>/home/jhonatt/repos/jhonatt_projects/dragaocare-admin-api/src/scripts/verify-episode-artifact-downloads.ts, /home/jhonatt/repos/jhonatt_projects/dragaocare-admin-api/src/services/episode-artifact-preparation.service.ts, /home/jhonatt/repos/jhonatt_projects/dragaocare-admin-api/src/database/repositories/artifact-job.repository.ts</files>
  <action>Finish the verifier’s OpenAPI parity checks for all three exact paths, POST requestBody array schema and selector enum, four-state status schema, progress bounds, public fields, bearer security, no-store, binary content, safe Content-Disposition, X-Missing-Artifacts, and documented 400/401/404/409 responses. Add regression assertions for repository state-transition constraints using the SQLite/episode-repository conventions, and for cleanup not deleting another job’s archive. If implementation corrections are needed, keep them limited to the job service/repository symbols established in Plan 01: map failed status errors without exposing internals, make expiry lookup return unknown/404, and validate a persisted archive path against the job-specific root before streaming. Run the full API verification set and the web build from `/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api` and `/home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-web` so Phase 4’s cross-repository handoff records that frontend contract assumptions remain documentation-only in this phase.</action>
  <verify>
    <automated>cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api && npm run typecheck && npm run build && npm run verify:episode-artifact-downloads && cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-web && npm run build</automated>
  </verify>
  <done>OpenAPI/runtime parity and repository security regressions pass; the sibling API verifier and required frontend build are green; no Angular implementation files were added for Phase 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verifier fixtures -> API service/routes | Test data intentionally simulates hostile selectors, IDs, auth states, and filesystem conditions. |
| Persisted job metadata -> cleanup/download checks | A stale or tampered row must not permit cross-job deletion or arbitrary streaming. |
| API responses/logs -> operator/client | Public evidence must not expose internal paths, cache keys, or raw errors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | job enumeration verifier | high | mitigate | Assert unknown and mismatched IDs produce indistinguishable 404 results and no internal fields or path-bearing logs. |
| T-04-03-02 | Elevation of Privilege | authenticated download verifier | critical | mitigate | Invoke all three routes without authorization and assert 401 before any archive access; separately verify valid bearer/bypass access. |
| T-04-03-03 | Denial of Service | retention and failure cleanup | high | mitigate | Use isolated job roots, forced failure, expired timestamps, and cross-job cleanup assertions to prove partial/final files are bounded and scoped. |
| T-04-03-04 | Tampering | invalid selector/path cases | critical | mitigate | Send filename/path-shaped and unknown selector values through JSON and assert 400 plus zero job creation. |
| T-04-03-05 | Tampering | archive traversal | critical | mitigate | Inspect ZIP central-directory names and assert every entry is catalog-derived beneath the expected episode prefix with no traversal segments. |
| T-04-03-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages are introduced; full verification reuses the existing API dependency set. |
</threat_model>

<verification>
The phase gate is `cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-api && npm run typecheck && npm run build && npm run verify:episode-artifact-downloads`, followed by `cd /home/jhonatt/repos/jhonatt_projects/dragaocareca-admin-web && npm run build`.
</verification>

<success_criteria>
VAL-04 has executable evidence for creation, all four lifecycle states, monotonic progress, completion/download, failure/cleanup, partial/missing reporting, duplicate protection, restart recovery, expiry, auth/ownership, invalid selector/path rejection, archive traversal safety, and OpenAPI parity.
</success_criteria>

<output>
Create `.planning/phases/04-artifact-job-contract/04-03-SUMMARY.md` when done.
</output>
