---
phase: 05-episode-download-modal
plan: 04
type: execute
wave: 1
depends_on: [05-03]
files_modified:
  - src/app/pages/manage/manage.component.ts
  - src/app/pages/manage/manage.component.spec.ts
autonomous: true
gap_closure: true
requirements: [UI-06]
must_haves:
  truths:
    - "A second confirmation made before the first artifact-job start Observable emits does not issue another start API call."
    - "The artifact start guard is released when the start Observable emits successfully or errors, while an active pending/processing job remains protected by the existing job-state guard."
    - "An asynchronous ManageComponent regression test proves two rapid confirmations result in exactly one artifact-job start request."
  artifacts:
    - path: "src/app/pages/manage/manage.component.ts"
      provides: "Per-episode in-flight artifact-job start guard around confirmArtifactJob"
      contains: "confirmArtifactJob"
    - path: "src/app/pages/manage/manage.component.spec.ts"
      provides: "Deferred-Observable duplicate-submission regression coverage for UI-06"
      contains: "Subject"
  key_links:
    - from: "src/app/pages/manage/manage.component.ts"
      to: "src/app/core/api.service.ts"
      via: "confirmArtifactJob sets the guard before invoking startEpisodeArtifactJob and releases it in next/error handlers"
    - from: "src/app/pages/manage/manage.component.spec.ts"
      to: "src/app/pages/manage/manage.component.ts"
      via: "a deferred start Observable keeps the first request unresolved while confirmArtifactJob is called twice"
---

<objective>
Close the only Phase 5 verification gap: prevent duplicate artifact-job starts during the interval after confirmation and before the first start response emits, and prove the race is covered by an asynchronous regression test.

Purpose: Satisfy UI-06 and D-12/D-15 at the client start boundary without changing the API contract, polling behavior, Add episode workflow, or Phase 6 browser delivery boundary.
Output: In-flight start protection in ManageComponent plus deterministic deferred-Observable coverage.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-episode-download-modal/05-CONTEXT.md
@.planning/phases/05-episode-download-modal/05-VALIDATION.md
@.planning/phases/05-episode-download-modal/05-VERIFICATION.md
@.planning/phases/05-episode-download-modal/05-03-SUMMARY.md
@docs/README.md
@docs/ARCHITECTURE.md
@docs/CONFIGURATION.md
@src/app/core/api.service.ts
@src/app/pages/manage/manage.component.ts
@src/app/pages/manage/manage.component.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard artifact-job starts before the Observable emits</name>
  <files>src/app/pages/manage/manage.component.ts</files>
  <action>Implement a dedicated in-flight start marker for the artifact-job confirmation path, scoped to the episode being confirmed. Check this marker at the beginning of confirmArtifactJob alongside the existing modal, selection, and active-job guards; set it before invoking apiService.startEpisodeArtifactJob so a second rapid confirmation is rejected even when the first Observable has not emitted. Release the marker in both the start subscription next and error handlers, using the episode identity so a stale callback cannot clear a newer start. Preserve the existing next behavior that stores the snapshot and ensures one poller, preserve unavailable/error messaging, and retain the existing active pending/processing protection for D-12 and D-15. Keep the guard isolated from Add episode editor/upload/transcription/summary state and do not add native ZIP delivery or any new API request. Cite UI-06, D-12, and D-15 in the implementation rationale or test-facing naming where the guard behavior is enforced.
  <verify>
    <automated>rg -n "confirmArtifactJob|startEpisodeArtifactJob|artifact.*(in.?flight|pending)|isArtifactJobActive" src/app/pages/manage/manage.component.ts</automated>
  </verify>
  <done>confirmArtifactJob marks the start request before subscription/emission, ignores rapid duplicate confirmations for the same episode, and clears only that in-flight marker on success or error while leaving active-job/poller behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add the asynchronous UI-06 duplicate-start regression</name>
  <files>src/app/pages/manage/manage.component.spec.ts</files>
  <action>Replace or augment the current synchronous duplicate-start assertion with an asynchronous Observable controlled by a Subject or equivalent deferred test source. Open the existing episode modal, select the same artifacts, call confirmArtifactJob twice before emitting the first start snapshot, and assert startEpisodeArtifactJob was called exactly once. Emit a pending/processing snapshot afterward so the test also proves the normal next path stores the job and starts polling; add a focused error-path assertion if needed to demonstrate that an errored start releases the guard for a later legitimate confirmation. Keep the existing UI-06 progress/partial assertions intact, use the established Jasmine/fakeAsync/TestBed style, and do not add package dependencies or browser-download behavior.
  <verify>
    <automated>npm test -- --watch=false --browsers=ChromeHeadless --include='src/app/pages/manage/manage.component.spec.ts'</automated>
    <automated>npm run build</automated>
  </verify>
  <done>The focused ManageComponent suite contains an asynchronous rapid-confirmation regression that fails without the pre-emission guard and passes with exactly one start API call; the suite compiles and the Angular build remains successful.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User confirmation events → ManageComponent start state | Rapid clicks or keyboard confirmations mutate client-side request state before the backend response exists. |
| ManageComponent → authenticated ApiService | The guarded component submits canonical artifact selectors to the existing authenticated HTTP boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-12 | Denial of Service | `confirmArtifactJob()` | high | mitigate | Set an episode-scoped in-flight marker before calling `startEpisodeArtifactJob`, release it on next/error, and prove two deferred rapid confirmations produce one start call. |
| T-05-13 | Tampering | Artifact start guard and selector submission | medium | mitigate | Preserve the existing modal validation, fixed canonical selector mapping, active-job guard, and authenticated `ApiService` boundary; this closure changes only duplicate-start timing state. |
</threat_model>

<verification>
<automated>npm test -- --watch=false --browsers=ChromeHeadless --include='src/app/pages/manage/manage.component.spec.ts'</automated>
<automated>npm run build</automated>
<human-check>In a browser-capable environment, open an episode's artifact modal and activate confirmation twice rapidly; verify one job starts and the existing progress/polling state appears once.</human-check>
</verification>

<success_criteria>
- UI-06's duplicate-submission truth passes before the first start Observable emits.
- Start state is released on both successful emission and error, without weakening active pending/processing or polling guards.
- The asynchronous regression test is executable in the existing ManageComponent spec boundary.
- No files outside the two exact feature/test files are modified by this gap closure, and Phase 6 native ZIP delivery remains untouched.
</success_criteria>

<output>
Create `.planning/phases/05-episode-download-modal/05-04-SUMMARY.md` when done.
</output>
